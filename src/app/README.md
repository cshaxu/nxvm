# App

The command-line entry point and commands such as `run`, `inspect`, `debug`, and
`version` belong here. This module has no emulation logic. No Microsoft import
command is planned before an M6 Go decision.
