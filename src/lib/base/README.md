# base

`base` is the root component. It provides only scalar types, copied logical
Console values, and platform-neutral lifetime/output contracts. Its permitted
dependents are `ux-base`, `host`, `storage`, `ux-window`, and `ux-console`; it
has no component dependency.
