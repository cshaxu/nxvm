# types

`types` is the root component. It provides only scalar aliases, status values,
and portable internal atomic helpers. Every other component may depend on it;
it has no component dependency and contains no logical I/O object.
