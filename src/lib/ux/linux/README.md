# Linux UX Native Implementation

The Console presenter consumes only the public `ux_binding` and copied frame
mailbox.  Its process Console lease contains no product pointer.  A Linux
Window implementation is intentionally absent: this task does not invent one.
