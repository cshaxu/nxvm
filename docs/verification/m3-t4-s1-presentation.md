# M3 T4 S1 Presentation Verification

`nxvm-dos-minimal-presentation-smoke` printed
`M3:T4:S1:PRESENTATION:OK`. It queued a timestamped platform-facing key event,
applied it only through the runtime boundary, read it through the guest port,
and captured copied text state. A source scan found no platform reference to a
core Machine or private core header. M1 baseline remained buildable.
