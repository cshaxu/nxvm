# M5 T74 S1: Governance And Build Cleanup

- Replaced stale CMake presets with three GCC presets whose targets exist in
  the current CMake graph.
- Reconciled current M5 status, added the T64--T73 compact ledger and
  verification index, and archived the closed legacy, naming, and
  instance-authority plans.
- Repaired the historical architecture link and updated the test layout guide.
- Verified all relative Markdown links and JSON parsing; no runtime source or
  runtime-test file changed.
- No executable artifact applies because this is a governance/build-config task.
