# M5 T27 S1 PIC Authority

`pic.*` owns the retained master/slave PIC implementation. The VM live carrier
embeds both objects; `vpic1` and `vpic2` are direct aliases. GCC, PIC identity
and IRQ smoke, CPU-stop, full-PC FDD/HDD profile, and zero-edge DAG passed.
The local artifact is `build/output/nxvm-m5_t27.exe`, SHA-256
`982D9E538FD25E8FF07D65356D3A43C181DAA16465FA36CE81B9CD2C15D6446C`.
