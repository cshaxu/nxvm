# Verification Records

Each completed subtask adds a short English record with working revision, exact
commands, generated/local input identities, expected and actual markers,
automated result, manual result when required, and known limitations. Raw
traces, crash dumps, and protected screenshots stay under ignored `artifacts/`.
