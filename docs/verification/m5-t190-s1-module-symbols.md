# M5 T190 S1: Module-Owned Symbols

All active internal identifiers use `type`, `core`, `vm`, or `vdm` ownership
prefixes. The remaining `NXVM`/`NTVDM64` scan hits are product strings,
artifact names, project metadata, or historical comments.

GCC built `vm-0-5-0190`; direct `exit` preserved the NXVM banner and Console
prompt: `M5:T190:S1:SYMBOL-CLOSURE:OK`.

Artifact: `build/output/nxvm_0_5_0190.exe`

SHA-256: `BF2859CBB730B9DA53AE4C1BFEA2949291C59EF49CB02151DF4B4425708794C0`
