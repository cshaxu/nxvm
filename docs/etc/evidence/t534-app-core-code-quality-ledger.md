# T534 App/Core Code-Quality Convergence Ledger

This ledger is the finite disposition record for the code-quality remediation
task.  It is evidence, not a second current architecture authority.

| ID | Mechanism / affected variants | Initial evidence | Required end state | Disposition |
| --- | --- | --- | --- | --- |
| CQ-1 | App recorder command, lifecycle and Core debug observer | `record start` creates a writer, but production has no observer binding; only its smoke calls `vm_app_recorder_observe` | Remove the unsupported recorder as one complete mechanism | Repaired by S1; see `t534-s1-recorder-removal.md` |
| CQ-2 | Machine display conversion and latest-frame publication | Converter marks destination valid before rejecting a source; prior validity can survive failure | Failure-atomic conversion and one truthful publication result | Repaired by S2; see `t534-s2-presentation-single-path.md` |
| CQ-3 | Core presentation mailbox versus Common UI frame | Production publishes both; integration captures the Core-only mailbox while UI captures Common frame | One production presentation route; integration observes it | Repaired by S2; see `t534-s2-presentation-single-path.md` |
| CQ-4 | Profile resolution/provenance and board materialization | Recursive parent/field-owner resolver and generic Machine FDC/HDC board materializer remain live | Direct frozen profile plans; Profile owns board construction | Repaired by S3; see `t534-s3-profile-direct-plan.md` |
| CQ-5 | Root `type.*` host/file facade and App INI path loading | App INI bypasses Lib storage; facade retains platform/file helpers; root-directory edge case | One canonical host/file capability and correct path resolution | Repaired by S4; App reads through Lib storage, all unused file wrappers are deleted, and root path parsing is covered. |
| CQ-6 | Repeat App/Core audit | Initial audit is structural and key-path review, not proof of absence | Re-audit each completed batch and record every new finding/receiver | Pending |
| CQ-7 | Machine legacy lifecycle entry points | S6 removed the public unused facades, but the live Machine control still owned a direct pause-reason/step state and integration probes still bypassed Common to operate it | Delete the obsolete state and every caller; Common remains the sole composed lifecycle owner and Debug uses its established execution-plan path | Repaired by S6/S26: S6 `721bffb9` removed the public facade; S26 removes the residual backing state and migrates every affected probe to Common. |
| CQ-8 | App Machine/Control/UI composition | Short-circuit construction leaves objects owned by `vm_app` after a failed stage, so the caller cannot retry composition | Each layer rolls back only objects it created and restores its entry state | Repaired by S8 `c736e749`; a repository-only fake boundary forces each stage failure and proves same-App retry. |
| CQ-9 | Machine removable/fixed media facade and FDC diagnostic dump | A public HDD insertion API is an unconditional failure stub; floppy eject accepts and ignores a path; FDC exposes an uncalled stdout dump | Retain only supported media transitions with truthful parameters; remove the dead dump path | Repaired by S10; the no-argument eject operation is the sole removable-media exit, startup HDD attachment remains private, and the smoke creates its owned image through Lib storage. |
| CQ-10 | CPU watchpoint reporting | CPU execution writes directly to stdout while the retained Debug CLI already has a copied watchpoint-result presentation path, but Core does not populate it | Core publishes one copied watchpoint observation; Debug owns user-facing report text; no Core stdout route remains | Repaired by S11: read, write and execute hits each cause a post-retirement Core pause, one copied observation and the existing x86 Debug report. |
| CQ-11 | Fresh Unit target build after root-file facade removal | Two media smokes still call removed root file wrappers; seven generated-result runners still name the former wrapper types/functions | Media fixtures use the retained Lib reader/writer; generators use ISO-C stream output only at the test-only build-artifact edge | Repaired by S11: a clean complete unit-target rebuild succeeds; no removed root file wrapper call remains. |
| CQ-12 | Uncalled App/Core declarations and wrappers | Whole-tree declaration/caller sweep finds wrappers with only their declaration and definition: CPU segment loader, display mode notification, FPU profile/name/busy helpers, KBC output hook, firmware A20/stop requests, timing manifest count and plan memory-device registration | Delete dead wrappers and declarations; retain the lower-level owners that have live callers | S13 admitted for complete caller sweep and removal. |
| CQ-13 | App composition failure result | Machine, Common and UI composition coalesce every inner failure into `TYPE_STATUS_INVALID_STATE` | Preserve Type failures and translate each Lib failure once at the App boundary; rollback remains identical | Repaired by S14: Type failures remain exact and Common/UI Lib outcomes cross one local App mapping; retry rollback remains covered. |
| CQ-14 | Machine adapter result masking | VM Machine reconfiguration, reset-vector capture and information snapshot rewrite Core failure statuses as `INVALID_STATE` | Preserve the Core status at the adapter boundary; only local input/lifecycle preconditions report local status | Repaired by S15: all three adapters return their Core result unchanged after local validation; reset-vector argument classification is covered. |
| CQ-15 | Fixed-media capability and construction result | HDD claims removable/change-detectable although only startup attaches it; its detach accepts an ignored pathname; requested blank-disk allocation can fail while Machine creation reports success | Fixed media claims only its actual provider capabilities; detach has no false parameter; requested blank media either installs or makes construction fail | Repaired by S16: HDD advertises geometry/format/read-only facts only; its private detach is parameterless; `create` returns an allocation result and Machine maps a requested creation failure to `NO_MEMORY`. |
| CQ-16 | Floppy detach contract and blank-medium construction | FDD detach accepts and ignores a pathname; it is the same false-input shape S16 removed from HDD | FDD detach has only its actual input; blank-medium allocation remains exclusively in already-fallible FDD initialization | Repaired by S17: FDD detach is parameterless at every caller; `initialize_with_geometry` remains the sole allocation boundary and its result already reaches Machine construction. |
| CQ-17 | Core run cold-reset failure masking | Both stop-request run branches rewrite a firmware cold-reset failure as `FAULT` | Preserve the reset status through each run branch | S18 repaired; focused firmware capability smoke proves `UNSUPPORTED` survives. |
| CQ-18 | App Machine creation result | App composition rewrites every `vm_machine_create` failure as `INVALID_STATE` | Preserve the Machine-owned Type result through App composition | Repaired by S19: the result is now assigned directly; the composition retry smoke proves `NO_MEMORY` survives a failed create. |
| CQ-19 | Machine-to-x86 Debug adaptation | Every failed Core Debug operation is rewritten as `LIB_STATUS_INVALID_STATE` | Use one complete Type-to-Lib mapping at the sole adaptation boundary | Repaired by S19: one private mapper retains every shared classification and maps the residual Type `FAULT` to Lib operational failure; the paused-debug smoke injects and proves a Core `INVALID_ARGUMENT`. |
| CQ-20 | Machine runner terminal outcome | Non-fault Core-run, pacing and waiting-advance failures call normal stop, so the Common driver reports `STOPPED` rather than `ERROR` | Machine owns one private terminal-failure fact; every abnormal runner branch sets it and the existing boolean driver result reports failure | Repaired by S20: runner-owned `runner_failed` reaches the existing Common boolean driver result; focused proof forces a non-fault Core `INVALID_STATE` and observes `COMMON_MACHINE_ERROR`. |
| CQ-21 | Machine display-to-Common frame conversion | After caller-owned destination validation, every display update heap-allocates a complete temporary frame, copies into it, copies it again into the supplied destination, then frees it | Validate first, then populate the caller-owned destination once; rejected source must leave it unchanged | Repaired by S21: all validation precedes destination mutation; graphics and text now fill the supplied frame directly, with no temporary full-frame allocation/copy or allocation-failure branch. The focused frame smoke and 335/335 repository-only unit suite pass. |
| CQ-22 | Integration compatibility after App/Core retirement | Default build finds five `STD_FPUTS` and one `STD_FPUTC` use of deleted test-output facades, plus one use of deleted runtime request field `profile` | Integration tests use retained test-local `STD_FPRINTF` and compiled Machine profile configuration; production interfaces stay deleted | Repaired by S22: every retired output facade is replaced by the retained test-local formatter, and the Model 40 probe reads the Machine's frozen compiled profile kind. All three affected targets build; lifecycle integration and 335/335 repository-only unit tests pass. |
| CQ-23 | App teardown after Common worker failure | `vm_app_destroy` ignores `common_machine_shutdown` failure then frees its callback contexts although Common documents that its worker remains live | Preserve the App graph on shutdown failure and return the mapped status; release downstream owners only after shutdown succeeds | Repaired by S23: App returns the mapped shutdown result without releasing any owned object; the composition smoke proves failure leaves all owners live and a later successful destroy cleans them once. 335/335 repository-only unit tests pass. |
| CQ-24 | Active direct-control reset failure | Runner records a failed reset outcome but can continue to report normal completion, so its Common adaptation may classify the abnormal run as `STOPPED` | Route the recorded failure through the existing Machine runner-failure fact; preserve ordinary stop behavior | Repaired by S24: reset failure retains its existing outcome and now sets the sole `runner_failed` fact. The runner regression forces a live firmware reset failure and observes `COMMON_MACHINE_ERROR`; 335/335 repository-only unit tests pass. |
| CQ-25 | Direct Core creation output ownership | `core_machine_create()` validates configuration before clearing a valid out parameter, while plan creation and all peer creators clear it before validation | Every failure with a valid out parameter leaves that parameter null | Repaired by S25: the direct constructor validates the out parameter, clears it, then validates configuration. The Core time smoke starts with a non-null sentinel and proves invalid configuration clears it; 335/335 repository-only unit tests pass. |
| CQ-26 | Default RAM construction | `core_machine_memory_initialize()` is a void convenience wrapper that allocates 16 MiB then discards the allocation result | Delete the implicit, failure-masking wrapper; every owner-local fixture requests its size through the existing fallible constructor | Repaired by S27 `ca28805e`; all 18 callers now observe `initialize_for()`. |
| CQ-27 | Default FDD construction | `vm_machine_fdd_initialize()` is a void convenience wrapper that creates a default-geometry zero overlay then discards its allocation result | Delete the implicit, failure-masking wrapper; every owner-local fixture requests a geometry through the existing fallible constructor | Repaired by S28 `208b0b40`; every FDD construction caller observes its fallible result. |
| CQ-28 | Profile/Machine constructor output ownership | File-backed Profile-plan and public Machine wrappers validate input or load assets before nulling a valid output pointer | Every valid output pointer is cleared before any remaining validation or asset failure | Repaired by S29 `9084b298`; Profile/Machine creation clears every valid output before later failure. |
| CQ-29 | App INI load failure classification | `vm_app_ini_load()` returns `FAULT` for invalid caller arguments and `NO_MEMORY` from Lib/document allocation | Preserve invalid-argument and no-memory results at the sole App INI loading boundary | Repaired by S30; null caller arguments are `INVALID_ARGUMENT`, allocation failure is `NO_MEMORY`, ordinary file failure remains `FAULT`. |
| CQ-30 | Profile BYOB asset loading | Blob and private fixed/variable file loaders collapse invalid input and Lib `NO_MEMORY` into `FAULT`; private outputs can retain stale values after failure | Preserve failure class and establish clear private output state before every later validation or asset failure | Repaired by S31; the entire loader family preserves `INVALID_ARGUMENT`/`NO_MEMORY`, retains `FAULT` for external invalidity, and clears private outputs first. |
| CQ-31 | Disabled legacy development trace | Normal builds define `TYPE_TRACE_ENABLED=0`, yet CPU context still allocates a legacy trace stack and `_new_code_path_` still checks it | Remove the nonfunctional trace state, allocation and calls; preserve decoder error flow and the product debugger | Repaired by S32; the attempted trace-enabled compile proved the dormant implementation was already broken, so it was deleted rather than preserved as a false capability. |
| CQ-32 | App console process outcome | Void console entry discards INI, session-composition and Common runner failures, allowing `main()` to return success | Propagate one App status to the sole process exit decision | Repaired by S33: one App outcome now reaches `main()`; the missing-INI process probe proves nonzero exit, while the focused INI smoke and complete 334-case repository-only unit suite pass. |
| CQ-33 | App monitor command copy | Common validates and delivers up to `COMMON_SESSION_TEXT_CAPACITY - 1` bytes, but the ordinary App command path copies into a 256-byte buffer with silent `snprintf` truncation | One App copy capacity equals the accepted Common line capacity, so product parsing receives the complete valid line | Repaired by S34: the sole App command copy uses `COMMON_SESSION_TEXT_CAPACITY`; the caller sweep finds no smaller App monitor copy, and focused command plus complete 334-case repository-only unit proof pass. |
| CQ-34 | App configuration and INI outputs | Public configuration and INI entry points either validate before clearing a valid output or parse directly into it, exposing stale or partial caller state on failure | Clear every valid output before remaining validation, construct INI requests locally, and publish only a complete result | Repaired by S35: public App configuration/INI outputs clear before later failure; INI parsing uses one local request and publishes only on success. Focused failure-state assertions and the complete 334-case repository-only unit suite pass. |

## Completion Predicate

Every row is repaired with code/test evidence, or transferred with a named
receiver and reason.  The final audit must find no unclassified live App/Core
duplicate owner, reverse composition dependency, disconnected operation or
failure-atomicity violation.
