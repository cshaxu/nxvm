# T391 S6: Contained C1 Transfer Replay

`M5:T391:S6:C1-REPLAY:OK`

One owner-managed transient replay of the accepted test-only C1 capture
completed its two-million-retirement containment budget. It observed the C0A
predecessor and retained 1,981,742 classified successful C1 records across 44
normalized forms, with zero source-unallocated records and no fault/status
terminal. The `0:7C00` boot-transfer boolean was not reached.

The replay output, including all per-form lines, was transient and not
retained. No asset identity, path, digest, byte, PC or trace is recorded here.
The external-input runner initially rejected the new C1 diagnostic flag because
its test-only whitelist had not been updated; S6 corrects that same local
parser and the canonical synthetic test continues to cover all C0/C0A/C1 modes.

This is a bounded absence for the named C1 endpoint, not a firmware failure,
board-time, physical-retirement or L3 conclusion. The next receiver must
perform a complete disposition of the finite 44-form C1 aggregate before any
CPU qualification decision.
