# M4 MyNes Identity Rename

## Outcome

Apply the MyNes/mynes product identity consistently across tracked
project source, build/test identifiers, packaged executable names, documents,
governance tooling and task records. Preserve the existing emulator behavior,
version and artifact pairing.

## Acceptance

All tracked text under the repository has no remaining retired product identifier.
CMake configures and builds the MyNes x64/x86 product
and test graph; the focused and registered test suites pass; the paired MyNes
artifacts are present with the adjacent mynes.ini. Owner ROMs remain untracked
and ignored beneath assets/roms.
