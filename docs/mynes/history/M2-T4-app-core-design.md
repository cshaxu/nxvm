# M2 T4: App/Core Design

## Request And Delivery

Following M1 closure 5d70a12, the owner admitted M2 and App/Core design, then
requested complete design before stopping for interactive verification and
explicitly required authoritative archives. No App/Core source was authorized.
S1 delivery 39435f4 and acceptance d2f5093 established architecture and the
design-only delivery state. The main history was omitted at admission; this
record repairs that omission without changing the immutable S1 record.

S2 consumes the complete [D01-D15 ledger](../etc/app-core-verification.md), not
only the first discovered integration issue. It supplies concrete public and
debug contracts, commands/configuration, CPU/bus/cartridge behavior and future
device/presentation/input/audio/persistence ownership. The
[detailed evidence](../etc/evidence/m2-app-core-design.md) records source
inspection, corrections, numeric checks and the S1-to-S2 disposition change.
The [reference register](../etc/hardware-references.md) distinguishes original
MOS archives, NES/Visual6502 hardware research, actual shared ABI and policy.

## Scope And Proof Boundary

Every design domain has a disposition and named future proof receiver. M2 fixes
all 151 documented opcodes, strict basic NROM, finite debug and cooked management;
later device contracts do not admit M3-M5 tasks. The consolidated M2 execution
candidate retains cartridge, CPU and App integration scope and awaits owner
interaction. It receives no task identifier or source implementation here.

The unchanged shared corpus remains the actual product-source baseline. Lib
audio and atomic-save capabilities are explicit later upstream prerequisites.
ASCII paths, approximate palette, strict ROM profile, power versus warm reset,
guest traps versus host failure and resume-time output switching are reviewable
product decisions. Design delivery claims no runtime acceptance or game support.

Full documentation governance, whitespace and numeric consistency checks pass.
Compared with d2f5093, src/, test/, tools/ and root build files have zero delta;
no source/test scaffolding, binaries, external media or new runtime tests exist.
No remote is configured; local-only delivery follows the owner's standing rule.

## Process Retrospective

S1 also changed Execution's product-design policy inside T4 even though rule
edits require standalone Td admission. The historical action is not relabeled
as compliant. S2 changes no rule or gate. A bounded milestone-local Td review
will reconcile that policy's scope and prevention before final handoff; it
cannot retrospectively manufacture prior authorization or admit implementation.

## Coordinator Acceptance

Reviewed actual S2 delivery 619ac8b after switching to coordinator role, including
all new contracts/reference data, modified authorities, three retired proposals
and their consolidated receiver. The complete D01-D15 ledger resolves the S1
pending design rows. Original user outcomes map to M1 closure, admitted M2,
completed App/Core design and an explicit stop before implementation. No
unresolved design-critical row is hidden by a runtime test claim. Documentation
gate and diff checks pass; source/build/test baseline remains unchanged.

Accept S2 and close T4's design delivery, retaining the
[proposal companion](M2-T4-app-core-design-proposal.md). This acceptance is the
coordinator's delivery check, not the owner's interactive design approval. M2
remains open. The sole implementation candidate awaits that conversation and
separate admission. There are no task-generated build trees or binaries to clean.

## S3 Owner-Requested Reopening

After style revision 4717553, the owner rejected the separately named core/host
layer and explicitly reopened T4 for proposal/file-tree confirmation. S3 is the
next unused corrective subtask of the latest numeric task; prior closure evidence
remains historical. This delivery consolidates driver/media/debug/input/display
and future audio/pacing integration alongside deterministic hardware in machine.
It updates proposed driver handles, headers, build groups, tests and the
[receiving proposal](../etc/m2-execution-management-context.md). The same Common
executor/media/debug/frame contracts remain in force. No runtime code changes.

The complete D01-D15 inventory is retained; this correction affects D01/D02,
D08-D12 and D14 ownership/path descriptions without dropping any behavior or
acceptance obligation. Owner confirmation of the revised tree is pending; this
delivery does not claim final T4 reclosure or implementation admission.

S3 owner review of P1 further rejected test/support and the nested Core directory.
The explicit clarification requires both src/core and test/core to be flat.
The consolidated correction updates source/header paths, tests, proposal and
the active packet. NXVM's own reference path remains unchanged; its style is
adopted without its directory depth. Test roots are exactly app/core/common/lib/
integration. Shared code/tests remain untouched and no source is implemented.

## S3 Final Acceptance And T4 Closure

The owner explicitly requested T4 closure after correcting the source/test tree.
Coordinator reviewed actual deliveries 2695fec and 26aa436, including the proposed
interfaces, component/build boundaries, flattened tree and receiving proposal.
The original request and both review corrections are accounted for. Accept S3
and close T4; the D01-D15 design inventory remains complete at design level.

Documentation governance and whitespace checks pass. Runtime source/test/tools/
build diff since 4717553 is zero; no runtime tests or artifact claims are added.
No temporary build or asset cleanup is needed. Local-only delivery applies
because no remote is configured. The final acceptance P records closure only.

M2 remains open. Queue contains one approved unnumbered implementation candidate,
NES execution and management, covering real App/Core composition, cartridge
media, CPU/bus and debugging with x64/x86 proof. No implementation packet is
admitted by this closure. M3-M5 remain unadmitted with no T decomposition; the
unplanned-debt ledger contains no entries.
