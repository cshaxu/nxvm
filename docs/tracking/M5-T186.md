# M5 T186: Remove Wait Sleep Alias

`core_product_utils_sleep()` is removed. All callers now use the explicit
wait-scope dispatch API, `core_product_wait_milliseconds()`; the scope/provider
abstraction is unchanged.

Verification: `M5:T80:S6:WAIT-SCOPE:OK`, retained
`M5:T96:S1:CONSOLE-LIFECYCLE:OK`, GCC artifact, and a source scan with no
legacy alias callers.

Artifact: `build/output/nxvm_0_5_0186.exe`

SHA-256: `8AB5EE9343C4DB93896A08FC21F72DFC203804B787CFF439CE314E3A7027E462`
