# M3 T1: Define Shared Core Contract

## Status

Complete: S1 defined the Shared Core V1 contract skeleton. No baseline behavior
moved.

## Scope

- create module-local shared-core contract headers and minimal C skeletons;
- add compile/link smoke coverage;
- prove the contract has no DOS, product CLI, Win32, or firmware dependency;
- avoid top-level SDK-style `include/` layout.

## Evidence

- Subtask: `docs/planning/subtasks/m3-t1-s1.md`.
- Verification: `docs/verification/m3-t1-s1-shared-core-contract.md`.

## S2

- Complete: core Machines now own independent CPU reset state, checked RAM,
  explicit A20 state, and session-local typed port dispatch. The unmodified M1
  baseline remains the temporary full-PC regression adapter. Subtask:
  `docs/planning/subtasks/m3-t1-s2.md`. Verification:
  `docs/verification/m3-t1-s2-machine-instance.md`. Provenance:
  `docs/provenance/m3-t1-s2-machine-instance-shell.md`.
