# M5 T165: Transactional VM RAM Reconfiguration

`DEVICE ram` now routes through `vm_session_reconfigure_memory`: RAM capacity
is passed to core creation, the existing `vm_session` identity is retained,
and a completed configuration resets at the normal vector. Explicit FDD/HDD
paths are copied into session-owned storage and reattached during rebuild.

Evidence: `M5:T165:S1:SESSION-RECONFIGURE:OK`; explicit FDD DOS prompt remains
`M5:T70:S2:DOS-PROMPT:OK`. Artifact: `nxvm_0_5_0165.exe`.
