# M1 T2 Full NXVM Baseline

## S1

- P1: completed. Imported the complete NXVM `src/` tree at
  `6d6b7d70ab6ed83ab973d27aeea6db88f4e87e4f` into `src/nxvm-baseline/`,
  recorded per-file provenance, added the Windows GCC baseline target, and
  verified FDD/HDD execution traces. Verification:
  `docs/verification/m1-t2-s1-nxvm-baseline.md`.
- P2: preserved byte-identical upstream whitespace while excluding only the
  imported baseline path from Git whitespace checks. Project-owned paths remain
  checked normally.
