# M0 T12 Consolidate Research And Test Paths

## S1

- P1: consolidated optional Windows integration, Microsoft NTVDM component, and
  Win16 work into then-current research tasks; reserved a project-owned
  temporary NXVM Console `load` command as the pre-product DOS-test path; and
  merged redundant planning,
  requirement, corpus, and research documents.
  Verification: roadmap, policy, reference, link, and stale-term review. This
  starts no M1 implementation.

## S2

- P1: corrected `load` as a future project-owned pre-product Console command rather
  than an existing NXVM command; required it to delegate to the owned DOS
  loader; and split M1 into a GCC migration-design gate followed by baseline
  import/execution. Verification: cross-document terminology, link, and Git
  whitespace review. This starts no M1 implementation.

## S4

- P1: removed premature M3/M5 implementation subtasks. M2, M4, and M6 must
  respectively produce the approved M3, M5, and M7 Task/subtask breakdowns;
  each M8 corpus increment needs its own approved breakdown. Verification:
  milestone authority, stale-reference, link, and Git whitespace review. This
  starts no M1 implementation.

## S5

- P1: defined the ignored local executable-artifact convention for completed
  runnable subtasks, including stable naming, verification metadata, and the
  prohibition on treating a developer artifact as a release. Verification:
  policy, contributor guidance, M1 baseline artifact, and Git review.

## S6

- P1: defined the retained NXVM-machine banner, milestone/task/subtask suffix,
  Virtual DOS Machine identity-cutover conditions, and `DDDH` post-cutover
  version encoding. Verification: architecture, execution policy, baseline
  preservation, and Git review.

## S3

- P1: defined `load` as a constrained debugger command: only an empty paused
  debug session may load; command-line program selection or an already loaded
  program rejects it; success pauses before the first guest instruction; and
  reset is the only return to the loadable state. Verification: CLI, M5, roadmap,
  task, link, and Git whitespace review. This starts no implementation.
