# M0 T12 Consolidate Research And Test Paths

## S1

- P1: consolidated optional Windows integration, Microsoft NTVDM component, and
  Win16 work into M9 T1-T3; reserved a project-owned temporary NXVM Console
  `load` command as the pre-M7 DOS-test path; and merged redundant planning,
  requirement, corpus, and research documents.
  Verification: roadmap, policy, reference, link, and stale-term review. This
  starts no M1 implementation.

## S2

- P1: corrected `load` as a future project-owned pre-M7 Console command rather
  than an existing NXVM command; required it to delegate to the owned DOS
  loader; and split M1 into a GCC migration-design gate followed by baseline
  import/execution. Verification: cross-document terminology, link, and Git
  whitespace review. This starts no M1 implementation.
