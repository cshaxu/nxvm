# M4 T6: Define CPU Correctness And Bochx Verification

## S1

- Status: complete.
- Decision: CPU profiles claim only evidence-backed capabilities; `#UD` defects
  require bounded capture and a minimal project-owned probe. Bochx/Bochs is an
  optional local paired-step CPU research tool, never a product dependency.
- Evidence: historical NXVM Bochx README/API describes register/memory bridge,
  one-step execution, comparison, and linear-memory recording; the retained
  CPU source exposes the `#UD` path and partial `i386(...)` dispatch markers.
- Boundary: no Bochs source, patch, binary, BIOS, guest media, trace, or bridge
  code was imported. No CPU behavior changed.
