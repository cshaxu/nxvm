# M5 T143: Hygiene And Orphan-Model Closure

| Subtask | Scope | Status | Evidence |
| --- | --- | --- | --- |
| S1 | Remove isolated VM product media/presentation models and relocate VDM-only input event ownership | Complete | `docs/verification/m5-t143-hygiene-closure.md` |
| S2 | Replace bare source TODOs with priority-tagged ledger items | Complete | `docs/verification/m5-t143-hygiene-closure.md` |
| S3 | Rebuild current artifact and clean owned build outputs outside `build/output/` | Complete | `docs/verification/m5-t143-hygiene-closure.md` |

## Boundaries

- **Objective:** make source ownership, CMake targets, deferred-work records,
  and local artifact retention accurately describe the actual retained NXVM and
  VDM paths.
- **Non-goals:** change NXVM Console behavior, add VDM product behavior,
  implement the deferred CPU/PIT/I/O-map/debug work, or delete verified task
  artifacts below `build/output/`.
- **Rules:** source ownership/file naming, no smoke-only product model, source
  TODO priority/ledger rule, and build-tree hygiene in `RULES.md`.
- **Gates:** no old product-model or input-event symbols; GCC current/gates,
  retained Console, VDM presentation, CPU-probe, and FDD DOS-prompt checks;
  final artifact and build-tree inventory show preserved executables only under
  `build/output/`.
