# M5 T205: Console Adapter Cohesion Closure

Status: complete.

## Objective

Remove the one-consumer `selected_session.*` split and keep the typed selected
session borrow private to the cohesive Console machine adapter.

## Scope

- Move the typed manager borrow helper into `console_machine_adapter.c` as a
  `static` implementation detail.
- Delete `selected_session.c` and `selected_session.h`.
- Change the adapter closure gate to prove one direct composition borrow and
  reject a restored split, without depending on a helper source file.
- Advance the current task artifact to `0.5.0205`.

## Non-Goals

- Do not remove the Console provider vtable, duplicate borrow behavior into
  callbacks, change selected-session policy, or change Console/debugger UX.

## Rules And Stop Conditions

- `RULES.md`: retain cohesive code under one owner; split only for real
  ownership, lifecycle, policy, or independent test boundaries.
- `execution-policy.md`: current runtime smoke and static closure evidence
  remain distinct and must pass before completion.
- Stop for a changed callback result, debugger ordering, Console lifecycle,
  boot/media, selected-session, or two-session regression.

## Required Evidence

- The closure gate proves the adapter is the one direct borrow site and the
  split helper files are absent.
- Current GCC gates and a `0.5.0205` Console `EXIT` artifact pass.

## Verification

- The focused Console, lifecycle, debugger, session-manager, and two-session
  CTest subset passed 5/5; the adapter closure gate emitted
  `M5:T205:S1:CONSOLE-ADAPTER-CLOSURE:OK`.
- `cmake --build --preset current-gates-gcc --parallel 1` passed: 42/42 CTest
  smoke tests and 19/19 static/structure gates.
- `cmake --build --preset current-gcc --parallel 1` and piped Console `EXIT`
  passed with exit status 0. Artifact: `build/output/nxvm_0_5_0205.exe`,
  SHA-256 `5EF205323D9A774E11A1CA30F4120C755729696380807180EE85D3A0542BBB6F`.
