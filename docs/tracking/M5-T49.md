# M5 T49: VM Device File Naming

## S1

- Status: complete.
- Scope: rename VM-private `vcmos`, `vdebug`, `vfdd`, `vfdc`, `vhdd`, and
  `vhdc` file pairs to canonical owner names without behavior change.
- Evidence: `docs/verification/m5-t49-s1-vm-device-naming.md`.
