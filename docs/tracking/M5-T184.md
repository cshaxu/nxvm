# M5 T184: VM Session Owner Closure

T176--T183 leave `vm_session` with direct embedded owners. The only retained
owner pointers are opaque `core_machine` and explicitly lazy `debug_target`.
The new static closure gate rejects former device/control alias fields and
control heap allocation.

Verification:

- `verify-vm-session-owner-closure`
- `verify-dependency-dag`
- `verify-session-readiness`
- `M5:T96:S1:CONSOLE-LIFECYCLE:OK`
- `M5:T70:S2:DOS-PROMPT:OK` with `D:\fdd.img`

Artifact: `build/output/nxvm_0_5_0184.exe`

SHA-256: `73261475A6F6277E5A1C76616929718646217D4E152BEA624BB9BE36A2D79492`
