# M5 T26: Port Dispatcher Authority

## S1

- Status: complete.
- Scope: map and move the retained port dispatcher into canonical `port.*` and
  the sole VM live-machine carrier without changing device callback order.
- Result: `port.c/.h` contain the retained dispatcher; `machine_port.c/.h`
  retain the independent M3 port model. `vport` is a direct alias to embedded
  live storage. See `docs/verification/m5-t26-s1-port-authority.md`.
