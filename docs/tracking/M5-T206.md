# M5 T206: Current-Gate Graph De-Duplication

Status: complete.

## Objective

Keep one current-gate execution path for each smoke target and remove obsolete
CTest-launching custom targets.

## Scope

- Remove the standalone preset entry for `vm-session-reconfigure-smoke`, which
  remains a `run-current-smokes` dependency and registered CTest case.
- Remove the Console lifecycle and core lifecycle custom targets that only
  launch smoke executables already registered in current CTest coverage.
- Advance the current task artifact to `0.5.0206`.

## Non-Goals

- Do not remove any smoke executable, CTest registration, coverage, or
  historical task evidence; old target names remain historical record only.

## Rules And Stop Conditions

- `RULES.md`: preserve runtime behavior and use clear ownership; no new build
  wrapper is introduced.
- `execution-policy.md`: current gates must build and execute registered CTest
  coverage rather than merely link smoke binaries.
- Stop if CTest membership, smoke behavior, or current-gate execution changes.

## Required Evidence

- Source/preset scan confirms obsolete targets and duplicate preset entry are
  absent while retained smoke targets remain CTest-registered.
- Current GCC gates and a `0.5.0206` Console `EXIT` artifact pass.

## Verification

- Fixed-string source/preset scan confirms both obsolete custom targets and the
  standalone preset `vm-session-reconfigure-smoke` entry are absent.
- `ctest --test-dir build/mingw-gcc-x64 -N -R
  "^current\\.vm-session-reconfigure-smoke$"` confirms the retained CTest case.
- `cmake --build --preset current-gates-gcc --parallel 1` passed: 42/42 CTest
  smoke tests and 19/19 static/structure gates.
- `cmake --build --preset current-gcc --parallel 1` and piped Console `EXIT`
  passed with exit status 0. Artifact: `build/output/nxvm_0_5_0206.exe`,
  SHA-256 `60EEE0D2671073AB3B19567CDDD6266F70CAE74121B222FFC90C9F5D29139935`.
