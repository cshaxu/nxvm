# M5 T166: Default Media Policy

Session zero now starts with no implicit host media. The former machine-local
Windows and POSIX image paths are deleted; explicit `DEVICE` insertion remains
the only media selection route. Artifact: `nxvm_0_5_0166.exe`.
