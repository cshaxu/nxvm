# M0 T13: Dual-Product Governance Reset

## Status

Complete: S1 aligned governance to the shared-core dual-product direction. No
runtime behavior changed.

## Scope

- define the repository as the successor to NXVM with two first-class products:
  `nxvm.exe` and `ntvdm64.exe`;
- make `core`, `firmware`, `platform`, `runtime`, `dos`, and
  `products/{nxvm,ntvdm64}` the canonical ownership model;
- resequence milestones so firmware and `nxvm.exe` product design precede their
  implementation, and DOS/platform/product work keep design gates; and
- remove stale single-product wording from canonical governance documents.

## Evidence

- Design: `docs/architecture/overview.md`.
- Roadmap: `docs/planning/roadmap.md`.
- Status: `docs/planning/status.md`.
- Verification: `docs/verification/m0-t13-s1-dual-product-governance.md`.
