# M4 T2: Define Firmware Service Contracts

## S1

- Status: complete.
- Decision: Firmware V1 is a session-owned, frozen-before-reset registry of
  typed POST, ROM, and interrupt services. It uses only Machine contracts and
  declared abstract host capabilities.
- Evidence: the baseline's `vbiosAddPost`/`vbiosAddInt` registration and reset
  construction are mapped to the typed target contract in
  `docs/history/m0-m4/firmware-nxvm.md`.
- Boundary: no firmware source, host integration, DOS decision, or process CLI
  was added.
