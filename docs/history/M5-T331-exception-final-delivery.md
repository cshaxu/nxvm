# M5 T331: Exception Final-Delivery Construction

## Scope

T331 addresses a bounded instance of the holistic execution-path construction
debt: final exception delivery in `ExecFinal`. S1 owns the real-mode paths
currently deliverable from that function. It records the mask-to-vector,
restart-frame, fault-restoration, diagnostic, and terminal contracts before
converging equivalent construction. Protected delivery, serializers, return
paths, and unadmitted real-mode producer classes remain explicit transfers.

## Admission

The owner approved T331 in single-session mode on 2026-08-12 from the first
ordered candidate in `states/QUEUE.md`. The active S contract is authoritative
in `states/CURRENT.md`.

## S1 Matrix And Construction Result

The [S1 final-delivery matrix](../etc/evidence/t331-s1-real-exception-final-matrix.md)
records the real-mode producer evidence, delivery and terminal outcomes, and
the protected or unproven paths transferred outside this bounded S. It also
records the single private `ExecFinal` construction boundary that replaced the
duplicated real-mode tails without merging Intel-defined vector or frame
semantics.

## Closure

S1 completed in `0b3ca98d` with developer artifact `vm-0-5-0331` SHA-256
`DEC95998E27E5F4FAF05D5C7EAFCE62DB0C507D3E9E0EE27A4D46FD513BE18D3`.
Fresh GCC configuration, the construction verifier, focused owner and retained
producer regressions, documentation governance, diff check, and 212/212
current-gate tests passed. Paging requires protected mode, so real-mode `#PF`
is an architectural non-applicability rather than an unimplemented producer.
