# M5 T345: Direct-Compilation Strictness Convergence

## Task Record

T345 consumes T344's truthful 305-row direct-compilation matrix.  Its purpose
is to turn the 175 deferred rows into a reproducible ownership and warning
work partition, then promote only project-owned targets whose direct source
commands can satisfy the existing four-option strict GCC policy.

## Active Plan

The active S1 packet owns the whole-graph ownership and diagnostic baseline.
Later S units may promote project-owned test and production cohorts only after
that baseline proves their direct target/source ownership.  Mixed or inherited
production work is retained as exact debt, never covered by a global flag or a
linked dependency.

## Completed Subtasks

| Subtask | Closed outcome |
| --- | --- |
| S1 | Classified all 175 T344 deferred direct source/target rows through a mechanical ownership verifier and actual Ninja-command warning audit: 121 project-owned test rows, 6 embedded production-test rows, 1 type-foundation row, and 47 mixed/inherited production rows. The audit found 225 warnings without compile errors; an unrelated non-current request-bridge smoke interface drift is an exact TODO transfer. |
| S2 | Promoted the 118 targets with direct C sources exclusively under `tests/` to target-local GCC `-Wall -Wextra -Wpedantic -Werror`. Eighteen baseline warnings in eleven owner sources were removed without suppression or test-contract change. A fresh 175-row audit records zero warnings for all 121 owner-test rows; the three owner-test rows that share mixed targets remain a named S3 transfer. |
