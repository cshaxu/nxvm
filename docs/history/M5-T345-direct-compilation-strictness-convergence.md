# M5 T345: Direct-Compilation Strictness Convergence

## Task Record

T345 consumes T344's truthful 305-row direct-compilation matrix.  Its purpose
is to turn the 175 deferred rows into a reproducible ownership and warning
work partition, then promote only project-owned targets whose direct source
commands can satisfy the existing four-option strict GCC policy.

## Active Plan

The active S1 packet owns the whole-graph ownership and diagnostic baseline.
Later S units may promote project-owned test and production cohorts only after
that baseline proves their direct target/source ownership.  Mixed or inherited
production work is retained as exact debt, never covered by a global flag or a
linked dependency.

## Completed Subtasks

| Subtask | Closed outcome |
| --- | --- |
| S1 | Classified all 175 T344 deferred direct source/target rows through a mechanical ownership verifier and actual Ninja-command warning audit: 121 project-owned test rows, 6 embedded production-test rows, 1 type-foundation row, and 47 mixed/inherited production rows. The audit found 225 warnings without compile errors; an unrelated non-current request-bridge smoke interface drift is an exact TODO transfer. |
| S2 | Promoted the 118 targets with direct C sources exclusively under `tests/` to target-local GCC `-Wall -Wextra -Wpedantic -Werror`. Eighteen baseline warnings in eleven owner sources were removed without suppression or test-contract change. A fresh 175-row audit records zero warnings for all 121 owner-test rows; the three owner-test rows that share mixed targets remain a named S3 transfer. |
| S3 | Promoted the three zero-warning, single-source production targets (`type-facade`, `core-product-utils`, and `vm-platform-requests`) to target-local strict GCC compilation. The production ledger accounts for every remaining mixed or embedded direct source with ownership, warning baseline, risk, and one future admission trigger; no linked library is claimed as direct coverage. |

## Closure Audit

T345's original request was to make the 175 deferred T344 direct compilation
rows actionable without global flags, inherited-source rewrites, or linked
dependency inference.  S1 created the actual-command ownership and warning
baseline; S2 promoted 118 pure test targets and removed the entire 18-warning
test cohort; S3 promoted the three independently owned, zero-warning
production targets; and S4 made every remaining production row mechanically
accountable.

The final fresh GCC matrix contains 305 direct commands: 251 directly compiled
with target-local `-Wall -Wextra -Wpedantic -Werror` and 54 deferred commands.
The T345 verifier retains all original 175 ownership keys, including 121
owner-test, six embedded-production-test, one type-foundation, two safely
separable production, and 45 mixed/inherited production rows.  Its S4 ledger
rejects a missing, duplicate, or unexpected entry among the 51 residual
production sources.  The final audit records zero warnings in the owner-test,
type-foundation, and safely separable cohorts; the 207 remaining warnings are
only in the named retained production domains.

The residual direct-production strict-compilation TODO links the complete
target/source, risk, and admission ledger.  It requires one bounded ownership
domain at a time and prevents a blanket inherited exemption.  The unrelated
request-bridge smoke drift also remains a separate exact TODO.  No residual is
left as an unclassified generic "later" item.

Fresh configuration, the actual Ninja matrix, the T345 negative self-test,
the warning audit, all 56 specialized gates, documentation governance,
`git diff --check`, and 218/218 current-gate tests passed.  The retained
proposal accompanies this record in history.
