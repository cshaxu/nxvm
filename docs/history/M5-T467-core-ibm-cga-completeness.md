# M5 T467 Core IBM CGA Completeness

## Active Record

T467 owns the selected IBM CGA completeness contract through the sole Core
VADP owner. S1 freezes its original-source and external-model Checklist 1
before code-gap analysis or implementation may begin.

## S1 Accepted Evidence

Checklist 1 contains the finite seventeen-row IBM-CGA universe. The local IBM
Options and Adapters scan is rendered-page verified and is the primary
authority; OCR is locator-only. 86Box and PCjs corroborate the selected CGA
digital route but do not override IBM. MAME's generic MC6845 and Bochs/QEMU's
later VGA implementations are explicitly non-authoritative for IBM-CGA glue.
Manual L3 governs the IBM/Motorola digital facts; Model-339's qualified,
bounded 86Box cadence is Other L3 through the existing Board-L3-capable VADP
receiver. Unqualified default data and the exact underdefined
physical/profile classes remain explicit L2. No external source, media or
production dependency was introduced.

## S2--S6 Accepted Evidence

S2 reconciles all seventeen rows to one VADP/Core/profile/snapshot path and
corrects the capability vocabulary: an existing construction-only receiver is
Board-L3-capable even when its selected value is Current data L2. S3 deletes
the 640-mode special case and completes the one IBM-CGA port grammar. S4
closes the intensity palette and output-disable effects through the existing
snapshot owner. S5 records Model-339's qualified Other-L3 cadence through the
same receiver; it does not invent an IBM wait formula. S6 proves reset,
firmware, guest mode changes and B8000h all converge on the same copied
snapshot route. No VADP mirror, renderer, profile callback, external source or
second memory route was added.
