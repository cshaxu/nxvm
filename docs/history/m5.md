# M5 Historical Summary

M5 is **open**. This summary condenses completed T1--T207 records; the current
closure authority is [M5 Closure Checklist](../planning/m5-closure-checklist.md).
The detailed records were removed from the working tree by `M5 Td S1` and remain
recoverable from Git history starting at
`a52b123b35414e9e4152d7f186d16456abc79515`.

## Completed Shape

- The runnable NXVM path is organized under `src/core/` and `src/vm/` with a
  non-runnable VDM skeleton under `src/vdm/`; root composition is product-owned.
- Core owns the synchronous executor, CPU/RAM/port bus, shared devices, and
  frozen lifecycle. VM owns full-PC devices, firmware/profile behavior, host
  integration, Console UX, and composition.
- The retained NXVM Console, debugger, boot path, FDD/HDD probes, session
  isolation, and text display/input route were preserved through the migration.
- CPU/FPU capability is explicit; default `80386 + no FPU` consumes x87 escape
  forms without misclassifying legal escapes as `#UD`. Complete 80386 and FPU
  emulation remain deferred.
- The completed PC-compatible slices are 8254 read-back, bounded 8042/KBC
  keyboard routing, and CGA text-controller state. Graphics, complete PIT/KBC
  behavior, native POSIX validation, and DOS execution remain deferred.

## Latest Runnable Baseline

T216 is the latest completed technical task. Devices now signal IRQs only via
`core_machine_pic_irq_source`: PIT IRQ0, KBC IRQ1, FDC IRQ6, and HDC IRQ14
retain their own asserted state while core owns controller-visible edge/level
delivery, IRR/ISR, masks, EOI, priority, vector selection, and cascade. The
PIC port smoke covers edge, level, mask, priority, IRQ14 cascade, and EOI.
Its developer artifact is `build/output/nxvm_0_5_0216.exe`; its SHA-256 is
`08466E84D0661BA55B1564563F1A2FE7E03D6927AFBCE55C27B3FEAE0971156B`.
All 52 current CTest smokes pass.

T215 is the latest completed technical task. It removes the generic
firmware-interrupt portal API, executor context storage, registration/freeze/
dispatch helpers, portal-only smoke, and closure gate after proving no VM,
VDM, or profile production consumer exists. The sole path for software
interrupts is CPU `INT` decoding followed by ordinary IVT delivery; a focused
smoke proves that contract for 8086 and 80386. Its developer artifact is
`build/output/nxvm_0_5_0215.exe`; its SHA-256 is
`BF2AE72415C9F4B61B3C80DC7B43A4500D66B5186A6C343F605CDA67B2B05EE7`.
All 51 current CTest smokes pass.

T214 is the latest completed technical task. CPU exceptions now promote the
core to `FAULTED` and return `TYPE_STATUS_FAULT` with `STOP_FAULT`, detail,
and linear PC before ordinary stop/reset processing. The VM runner captures
one session-owned copy of the result and first-fault diagnostic; retained
Console status and debugger entry consume that copy after the run boundary.
The fault remains inspectable until explicit reset, which clears both core and
session copies. Its developer artifact is `build/output/nxvm_0_5_0214.exe`;
its SHA-256 is
`17735838DD571F82486D0F2F8C52775F55B5EEC5E26691B9AA0E0CB2D5A3934E`.
The runner-level fault smoke and all 51 current CTest smokes pass.

T213 is the latest completed technical task. It replaces the default profile's
HDD F4/F5 helpers with a primary master-only ATA PIO controller at `1F0h`--
`1F7h` and `3F6h`, routed through core PIC IRQ14. The session-owned HDC owns
its task file, transfer state, image backend binding, and geometry view; ROM
`INT 13h` programs the same ports. The direct probe covers `ECh`, `20h`,
`30h`, invalid/no-media, status acknowledgement, and SRST; a bounded HDD-image
smoke reaches the active partition VBR after two PIO reads. Its developer
artifact is `build/output/nxvm_0_5_0213.exe`; its SHA-256 is
`335466E503226796AE80499A73D79C2D0718E74907AD18511164C988E77A4158`.
All 50 current CTest smokes pass.

T212 is the latest completed technical task. It retired the default profile's
video `INT F2h` portal: the admitted text `INT 10h` services now run through
the default ROM IVT handler over BDA, `B8000`, and CRTC state observed by core
VADP. The normal runner now publishes a copied display frame per bounded
256-instruction quantum rather than per instruction. Its developer artifact is
`build/output/nxvm_0_5_0212.exe`; the SHA-256 is
`5280BEF8C8BCBE2010C9735163B2562EB5F9EB76536484E328C3598ABD4B5BAF`.
All 48 current CTest smokes pass.

T211 retired the default profile's
boot-failure `INT F0h` portal: the ROM writes a profile-defined BDA POST report
after the existing key acknowledgement, and the VM session runner is the sole
consumer that requests core stop. Its developer artifact is
`build/output/nxvm_0_5_0211.exe`; the SHA-256 is
`0198B04D66DA3898FA8E354783181D924A08B736301672A00F640B772AE758A1`.
The closure gate and all 46 current CTest smokes pass. An exploratory no-media
error-display run reached the existing text-video path but not the final key
wait; T212 owns that video-path regression follow-up.

T210 retired the default profile's
keyboard `INT F1h/F3h` portals in favour of KBC -> IRQ1 -> default-ROM
`INT 09h` -> BDA -> default-ROM `INT 16h`. The developer artifact is
`build/output/nxvm_0_5_0210.exe`; its SHA-256 is
`8390A8DC64D8A7118D77BA6F780594F071FB56E0FCEB48BB3F570D0F333E21E1`.
The T210 closure gate and all 45 current CTest smokes pass.

T209 replaced the default profile's
QDX opcode override with frozen, ROM-origin-limited firmware `INT F0h-F5h`
portals and retained ordinary guest-IVT fall-through for every other case. Its
developer artifact is `build/output/nxvm_0_5_0209.exe`; the SHA-256 is
`A24DCAA1908BD60309D960D7261A315DF027DFB580B81532B8BF6E42668E9A99`.
The source closure gate and 45 current CTest smokes pass. The default remains
`80386 + no FPU`; the strict-8086 diagnostic correctly reaches a real 80186+
`C1 EA 04` instruction in the DOS image, while a focused 8086/80186 probe
verifies that boundary.

T208 retained behavior while making the default PC/AT profile declarative for
ROM mapping, firmware hook order, CMOS/FDC ports, and FDC IRQ/DMA routing. Its
historical developer artifact remains `build/output/nxvm_0_5_0208.exe`; the
SHA-256 is `B779B86A7459BAB78DB3E2AD7DEDCF5F724293C35B9F50339FACD909A95A7339`.

`M5 Td S1` consolidated 805 Markdown files into a 36-file current working set. It
removed completed task, verification, detailed provenance, and obsolete-plan
files from the working tree without changing source or runtime behavior.

`M5 Td S2` defined six daily documentation authorities: design principles, coding
standard, documentation standard, system architecture, roadmap, and debt
tracking. It added no product behavior or milestone closure claim.

`M5 Td S7` added execution workflow as the seventh authority. Its closure
audit verified the requested entry/navigation, requirement packet and state
machine, and docs-only concise scope; it changed no product behavior, build,
artifact, or M5 technical closure item.

The historical commit-subject labels `T208` and `T209` for these two
documentation-only changes are not implementation task identifiers and do not
reserve those numeric `T` values.

## Remaining M5 Work

M5 cannot close until every applicable closure-checklist item has fresh current
evidence or an owner-approved deferral. In particular, current artifact/hygiene
audit, source-ownership audit, retained NXVM runtime evidence, and TODO
classification must be reconciled against the current graph. M6 remains
ineligible until M5 closes.
