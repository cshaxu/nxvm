# M5 T35 S1: FDC Authority Map

Map `vfdc` state, its port registration, DMA/IRQ callbacks, and FDD links
before binding its sole controller object to the live machine. Preserve the
existing callback and lifecycle order.
