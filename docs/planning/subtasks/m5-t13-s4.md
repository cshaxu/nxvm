# M5 T13 S4: VM CPU-Probe Name Cutover

## Objective

Rename the VM-only finite CPU probe source/header and CMake/test names out of
the historical `baseline` label by `git mv`, preserving its probe bytes,
capture data, and `#UD` output exactly.

## Non-Goals

Do not alter CPU semantics, probe coverage, exception output, runtime behavior,
Console, debugger, or any composition dependency.

## Verification

Build the user-facing artifact and CPU-probe smoke. Run the finite CPU, FDD
lifecycle, FDD/HDD reset-vector, no-media Console, and delayed debugger gates.

## Stop Condition

Stop for owner approval before a probe semantic or user-visible behavior change.
