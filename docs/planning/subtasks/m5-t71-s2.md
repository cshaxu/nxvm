# M5 T71 S2: Display Provider Repair

Status: complete.

## Objective

Repair the default-profile display provider so its snapshot callback receives
the composition-owned profile context. Preserve the retained Console and video
firmware behavior.

## Required Evidence

- `snapshot_context` names the same profile context used by CGA firmware.
- The real `machineStart -> platformStart` path reaches guest `A:\>` and the
  host platform display frame shows that prompt within three seconds.
- Firmware, CGA, full-authority, and strengthened prompt/display gates pass.

## Result

The repaired artifact is `build/output/nxvm_0_5_0071a.exe` because the
initial `0071` filename remained locked during validation. Its SHA-256 is
`D63B4B8D12C83F67E7A98395B864CDA6A0BF369E54F2662C71D308ECE914460E`.
