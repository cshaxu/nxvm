# M5 T101 S1: Root Status Vocabulary

Status: complete.

## Objective

Rename `nxvm_core_status` and `NXVM_CORE_STATUS_*` in all active source and
tests to `ntvdm64_status` and `NTVDM64_STATUS_*`, then record the remaining
root `t_*` type/macro inventory for T107.

## Non-Goals

- Do not change enum values, status flow, type widths, or product behavior.
- Do not introduce compatibility aliases.
- Do not rename retained uppercase C-runtime wrappers or `t_*` aliases yet.

## Verification

1. The active source/test scan has no old status vocabulary.
2. `cmake --build --preset nxvm-current-gcc -j 4`
3. `cmake --build --preset nxvm-current-gates-gcc -j 4`
4. FDD DOS-prompt and retained Console checks using
   `build/output/nxvm_0_5_0101.exe`.

Stop on an enum/value difference, ABI-facing alias requirement, or any retained
NXVM behavior regression.

## Result

All active source/tests use `ntvdm64_status` and `NTVDM64_STATUS_*`. The T107
root alias/macro inventory is recorded in the verification evidence.
