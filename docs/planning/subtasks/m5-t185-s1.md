# M5 T185 S1: Remove Empty Console Provider Lifecycle Hooks

Remove the empty Console machine-provider initialize/finalize callbacks, their
vtable slots, and their only two call sites. Retain all selected-session
adapter callbacks and Console behavior.

## Verification

- GCC task artifact
- retained Console lifecycle gate
- source scan confirming no provider lifecycle slots or empty callbacks remain
