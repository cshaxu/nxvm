# M5 T117 S1: Composition Directory Convergence

Status: complete.

## Objective

Move every root `composition*` source/header into `src/vm/composition/` or
`src/vdm/composition/`. Update direct include paths, CMake source paths, and
the module-layout authority without changing composition logic or behavior.

## Guardrails

- Use `git mv`; do not copy or rewrite composition implementations.
- Preserve all exported `vm_composition_*` and `vdm_composition_*` symbols.
- Do not change NXVM Console, debugger, boot, device, or session behavior.

## Verification

Build `nxvm_0_5_0117.exe`, run all GCC gates, the FDD DOS-prompt smoke, and
the retained Console script. Delete disposable build trees afterward while
preserving `build/output/`.

All checks passed; see
[`m5-t117-s1-composition-directory.md`](../../verification/m5-t117-s1-composition-directory.md).
