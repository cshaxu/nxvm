# M5 T181 S1: Direct Default-Profile Firmware Ownership

Embed default BIOS, QDX, and default-profile context in `vm_session`. Preserve
their retained initialization/reset order and pass explicit addresses to
firmware/profile APIs.
