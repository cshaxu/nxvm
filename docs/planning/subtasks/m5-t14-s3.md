# M5 T14 S3: Core-To-VM Dependency Removal

## Objective

Remove the approved core-to-VM/default-profile dependencies through core
contracts and VM root-composition bindings, without changing retained NXVM
Console, debugger, boot/media, or task-artifact behavior.

## Initial Slice

Move the legacy `vmachine` lifecycle sequence by `git mv` from
`src/vm/machine/vmachine.c` to `src/vm/composition.c`. Preserve its init,
reset, refresh, and final statement ordering exactly; repair only its direct
source/CMake path. Its old header remains temporarily in place until the
following focused move. This relocation makes the existing composition logic
visible without claiming that the composition refactor is complete.

## Non-Goals

Do not alter a device implementation, provider callback, BIOS/POST/IVT order,
Console grammar, debugger behavior, platform input/output behavior, media
behavior, C type names, or executable identity. Do not create a VDM executable.

## Reference And Gates

The reference is `126eb22` and the approved map is
`../m5-t14-s2-migration-map.md`. Before the next source move, run the
dependency-DAG target, the relevant GCC build, the VM composition/profile
smokes, retained Console/debugger gates, and bounded FDD/HDD reset-vector
evidence when a full-PC path is affected.

## Stop Condition

Stop for owner approval before any required change to lifecycle ordering,
legacy global-state lifetime, BIOS/POST ordering, or an observable NXVM
interaction.

## Initial Slice Evidence

`src/vm/machine/vmachine.c` was moved unchanged to `src/vm/composition.c` and
the legacy lifecycle target path was repaired. The dependency source allowlist
decreased from 50 to 49 entries because composition is the permitted product
root. GCC configured successfully; `verify-dependency-dag`, retained Console,
full-PC profile with `../fdd.img ../hdd.img`, and default-profile with
`../fdd.img` passed. `nxvm-m5-t13` also rebuilt successfully with the moved
source. No new task artifact is due until M5 T14 itself completes.

The unchanged legacy execution loop and command-boundary owner then moved from
`src/vm/machine/device.c` to `src/vm/composition_loop.c`; only its CMake path
changed. The same dependency, Console, FDD/HDD profile, default-profile, and
task-artifact build gates passed. `device.h` remains temporarily at its legacy
machine path because Console/debugger callers still use its broad API; its
public control surface is the next audited boundary, not a mechanical header
move.

The unchanged start/reset/stop/resume and platform lifecycle wrapper then moved
from `src/vm/machine/machine.c` to `src/vm/composition_machine.c`. This removed
the direct machine-to-platform source edge, reducing the checked allowlist to
48 entries. The same focused GCC, dependency, Console, FDD/HDD profile,
default-profile, and task-artifact build gates passed.

The private lifecycle declaration then moved from
`src/vm/machine/vmachine.h` to `src/vm/composition.h`. Its only two callers,
the root composition lifecycle and loop sources, now include the root header.
The same focused gates passed; no source-edge count changed because this move
removes a same-module legacy header rather than a forbidden cross-owner edge.

The unchanged full-PC creator, media/bootstrap binder, input transport binder,
run entry, and teardown implementation moved from `src/vm/product/full_pc.c`
to `src/vm/composition_full_pc.c`. Its two product-to-machine/platform edges
were eliminated, reducing the checked allowlist to 46 entries. The focused
Console, FDD/HDD profile, builtin profile, VM session, task-artifact, and
dependency gates passed.

The unchanged full-PC session profile-selection and create/reset/destroy
implementation moved from `src/vm/product/full_pc_session.c` to
`src/vm/composition_session.c`. Removing its stale product-to-profile allowlist
entry reduced the checked set to 45 edges. The dependency, Console, FDD/HDD
profile, builtin profile, VM session, and task-artifact build gates passed.

The unchanged VM CPU probe implementation moved from
`src/vm/product/cpu_probe.c` to `src/vm/composition_cpu_probe.c`, removing its
product-to-machine edge and reducing the checked set to 44 entries. Its
expected `#UD` diagnostic and `M5:T1:S1:CPU-PROBES:OK` marker, along with the
dependency, Console, FDD/HDD profile, and builtin profile gates, passed.

The unchanged legacy session registry, firmware, core-machine, default-profile,
debugger, and presentation assembly moved from `src/vm/product/session.c` to
`src/vm/composition_session_model.c`. Its product-to-profile edge disappeared,
reducing the checked set to 43 entries. The session, dependency, Console,
FDD/HDD profile, and builtin profile gates passed.

The unchanged operational default-profile adapter moved from
`src/vm/profile/default_profile/default_profile.c` to
`src/vm/composition_default_profile.c`. Its profile-to-product edge disappeared,
reducing the checked set to 42 entries. Default-profile, session, dependency,
Console, FDD/HDD profile, and builtin profile gates passed.

The test-only VDM minimal session profile-selection and create/reset/destroy
implementation moved from `src/vdm/product/minimal_session.c` to
`src/vdm/composition_minimal_session.c`. Its two product-peer edges disappeared,
reducing the checked set to 40 entries. VDM session/presentation, dependency,
Console, and NXVM task-artifact build gates passed; no VDM executable or CLI
was introduced.

The VDM DOS-minimal presentation bridge and its composition-only header moved
from `vdm/platform` to `src/vdm/composition_presentation.{c,h}`. The two
presentation callers were repaired, eliminating the platform-to-machine edge
and reducing the checked set to 39 entries. VDM presentation/session,
dependency, and retained NXVM Console gates passed.

The composition-only VDM minimal-session header moved from
`vdm/product/minimal_session.h` to `vdm/composition_minimal_session.h`; its
implementation and smoke include were repaired. The dependency count remained
39 because this removed a same-module legacy header. VDM session/presentation
and retained NXVM Console gates passed.

The legacy default-profile BIOS registrations were moved out of core DMA, PIC,
PIT, and KBC plus VM CMOS, FDC, HDC, and video-adapter device implementations
and into the VM root composition. The POST registration order, INT vectors,
and the FDC-to-HDC INT 13 override order are preserved. CMOS reset and refresh
remain immediately after its registrations, as in the retained sequence. This
removed eight core/VM-to-default-profile edges, reducing the checked allowlist
to 31 entries. Dependency-DAG verification, GCC builds, PC-device, firmware,
default-profile firmware, retained Console, FDD/HDD full-PC profile, default
profile, and VM-session smoke gates passed. Existing legacy GCC warnings were
observed but no new build failure occurred.

The CPU instruction implementation no longer includes the VM device control
surface. Its former `deviceStop` call sites now set a core-owned pending-stop
request; VM composition consumes it immediately after the same machine-refresh
boundary and preserves the retained loop exit. The checked allowlist is now 30
entries. `nxvm-vm-cpu-stop-smoke` overwrites the reset vector with `UD2`, runs
the real device loop, and verifies both stopped state and consumed request with
`M5:T14:S3:CPU-STOP:OK`. The CPU probe, retained Console, FDD/HDD full-PC
profile, default-profile, GCC build, and dependency-DAG gates also passed.

The common legacy standard-type and compile-time settings header moved by
`git mv` from `core/platform/global.h` to root `type.h`. All consumers now
include `type.h`; the move preserves existing type widths, trace switches, and
host-platform selection without changing runtime behavior. It removes four
core-to-platform source edges and leaves 26 checked temporary edges. GCC,
core-contract, presentation/debugger, retained Console, CPU-stop, FDD/HDD
full-PC profile, default-profile, and dependency-DAG gates passed.

The full-PC default-profile adapter header moved by `git mv` from
`vm/profile/default_profile/default_profile.h` to
`vm/composition_default_profile.h`, matching its root-composition
implementation and product-media dependency. Its API and data layout are
unchanged; direct callers and the focused smoke include now use the composition
path. This removes the profile-to-product edge and leaves 25 checked temporary
edges. GCC, default-profile, VM-session, retained Console, FDD/HDD full-PC
profile, and dependency-DAG gates passed.

The VM session assembly header moved by `git mv` from `vm/product/session.h`
to `vm/composition_session_model.h`, matching the root-composition session
implementation. It continues to own registry, firmware, media, profile, and
debugger assembly while VM product remains UX-only. The session API and layout
are unchanged; its direct implementation and smoke include now use the root
path. This removes the product-to-profile edge and leaves 24 checked temporary
edges. GCC, VM-session, default-profile, retained Console, FDD/HDD full-PC
profile, and dependency-DAG gates passed.
