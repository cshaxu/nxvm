# M5 T14 S3: Core-To-VM Dependency Removal

## Objective

Remove the approved core-to-VM/default-profile dependencies through core
contracts and VM root-composition bindings, without changing retained NXVM
Console, debugger, boot/media, or task-artifact behavior.

## Initial Slice

Move the legacy `vmachine` lifecycle sequence by `git mv` from
`src/vm/machine/vmachine.c` to `src/vm/composition.c`. Preserve its init,
reset, refresh, and final statement ordering exactly; repair only its direct
source/CMake path. Its old header remains temporarily in place until the
following focused move. This relocation makes the existing composition logic
visible without claiming that the composition refactor is complete.

## Non-Goals

Do not alter a device implementation, provider callback, BIOS/POST/IVT order,
Console grammar, debugger behavior, platform input/output behavior, media
behavior, C type names, or executable identity. Do not create a VDM executable.

## Reference And Gates

The reference is `126eb22` and the approved map is
`../m5-t14-s2-migration-map.md`. Before the next source move, run the
dependency-DAG target, the relevant GCC build, the VM composition/profile
smokes, retained Console/debugger gates, and bounded FDD/HDD reset-vector
evidence when a full-PC path is affected.

## Stop Condition

Stop for owner approval before any required change to lifecycle ordering,
legacy global-state lifetime, BIOS/POST ordering, or an observable NXVM
interaction.

## Initial Slice Evidence

`src/vm/machine/vmachine.c` was moved unchanged to `src/vm/composition.c` and
the legacy lifecycle target path was repaired. The dependency source allowlist
decreased from 50 to 49 entries because composition is the permitted product
root. GCC configured successfully; `verify-dependency-dag`, retained Console,
full-PC profile with `../fdd.img ../hdd.img`, and default-profile with
`../fdd.img` passed. `nxvm-m5-t13` also rebuilt successfully with the moved
source. No new task artifact is due until M5 T14 itself completes.

The unchanged legacy execution loop and command-boundary owner then moved from
`src/vm/machine/device.c` to `src/vm/composition_loop.c`; only its CMake path
changed. The same dependency, Console, FDD/HDD profile, default-profile, and
task-artifact build gates passed. `device.h` remains temporarily at its legacy
machine path because Console/debugger callers still use its broad API; its
public control surface is the next audited boundary, not a mechanical header
move.

The unchanged start/reset/stop/resume and platform lifecycle wrapper then moved
from `src/vm/machine/machine.c` to `src/vm/composition_machine.c`. This removed
the direct machine-to-platform source edge, reducing the checked allowlist to
48 entries. The same focused GCC, dependency, Console, FDD/HDD profile,
default-profile, and task-artifact build gates passed.

The private lifecycle declaration then moved from
`src/vm/machine/vmachine.h` to `src/vm/composition.h`. Its only two callers,
the root composition lifecycle and loop sources, now include the root header.
The same focused gates passed; no source-edge count changed because this move
removes a same-module legacy header rather than a forbidden cross-owner edge.

The unchanged full-PC creator, media/bootstrap binder, input transport binder,
run entry, and teardown implementation moved from `src/vm/product/full_pc.c`
to `src/vm/composition_full_pc.c`. Its two product-to-machine/platform edges
were eliminated, reducing the checked allowlist to 46 entries. The focused
Console, FDD/HDD profile, builtin profile, VM session, task-artifact, and
dependency gates passed.

The unchanged full-PC session profile-selection and create/reset/destroy
implementation moved from `src/vm/product/full_pc_session.c` to
`src/vm/composition_session.c`. Removing its stale product-to-profile allowlist
entry reduced the checked set to 45 edges. The dependency, Console, FDD/HDD
profile, builtin profile, VM session, and task-artifact build gates passed.

The unchanged VM CPU probe implementation moved from
`src/vm/product/cpu_probe.c` to `src/vm/composition_cpu_probe.c`, removing its
product-to-machine edge and reducing the checked set to 44 entries. Its
expected `#UD` diagnostic and `M5:T1:S1:CPU-PROBES:OK` marker, along with the
dependency, Console, FDD/HDD profile, and builtin profile gates, passed.

The unchanged legacy session registry, firmware, core-machine, default-profile,
debugger, and presentation assembly moved from `src/vm/product/session.c` to
`src/vm/composition_session_model.c`. Its product-to-profile edge disappeared,
reducing the checked set to 43 entries. The session, dependency, Console,
FDD/HDD profile, and builtin profile gates passed.

The unchanged operational default-profile adapter moved from
`src/vm/profile/default_profile/default_profile.c` to
`src/vm/composition_default_profile.c`. Its profile-to-product edge disappeared,
reducing the checked set to 42 entries. Default-profile, session, dependency,
Console, FDD/HDD profile, and builtin profile gates passed.

The test-only VDM minimal session profile-selection and create/reset/destroy
implementation moved from `src/vdm/product/minimal_session.c` to
`src/vdm/composition_minimal_session.c`. Its two product-peer edges disappeared,
reducing the checked set to 40 entries. VDM session/presentation, dependency,
Console, and NXVM task-artifact build gates passed; no VDM executable or CLI
was introduced.

The VDM DOS-minimal presentation bridge and its composition-only header moved
from `vdm/platform` to `src/vdm/composition_presentation.{c,h}`. The two
presentation callers were repaired, eliminating the platform-to-machine edge
and reducing the checked set to 39 entries. VDM presentation/session,
dependency, and retained NXVM Console gates passed.

The composition-only VDM minimal-session header moved from
`vdm/product/minimal_session.h` to `vdm/composition_minimal_session.h`; its
implementation and smoke include were repaired. The dependency count remained
39 because this removed a same-module legacy header. VDM session/presentation
and retained NXVM Console gates passed.

The legacy default-profile BIOS registrations were moved out of core DMA, PIC,
PIT, and KBC plus VM CMOS, FDC, HDC, and video-adapter device implementations
and into the VM root composition. The POST registration order, INT vectors,
and the FDC-to-HDC INT 13 override order are preserved. CMOS reset and refresh
remain immediately after its registrations, as in the retained sequence. This
removed eight core/VM-to-default-profile edges, reducing the checked allowlist
to 31 entries. Dependency-DAG verification, GCC builds, PC-device, firmware,
default-profile firmware, retained Console, FDD/HDD full-PC profile, default
profile, and VM-session smoke gates passed. Existing legacy GCC warnings were
observed but no new build failure occurred.

The CPU instruction implementation no longer includes the VM device control
surface. Its former `deviceStop` call sites now set a core-owned pending-stop
request; VM composition consumes it immediately after the same machine-refresh
boundary and preserves the retained loop exit. The checked allowlist is now 30
entries. `nxvm-vm-cpu-stop-smoke` overwrites the reset vector with `UD2`, runs
the real device loop, and verifies both stopped state and consumed request with
`M5:T14:S3:CPU-STOP:OK`. The CPU probe, retained Console, FDD/HDD full-PC
profile, default-profile, GCC build, and dependency-DAG gates also passed.

The common legacy standard-type and compile-time settings header moved by
`git mv` from `core/platform/global.h` to root `type.h`. All consumers now
include `type.h`; the move preserves existing type widths, trace switches, and
host-platform selection without changing runtime behavior. It removes four
core-to-platform source edges and leaves 26 checked temporary edges. GCC,
core-contract, presentation/debugger, retained Console, CPU-stop, FDD/HDD
full-PC profile, default-profile, and dependency-DAG gates passed.

The full-PC default-profile adapter header moved by `git mv` from
`vm/profile/default_profile/default_profile.h` to
`vm/composition_default_profile.h`, matching its root-composition
implementation and product-media dependency. Its API and data layout are
unchanged; direct callers and the focused smoke include now use the composition
path. This removes the profile-to-product edge and leaves 25 checked temporary
edges. GCC, default-profile, VM-session, retained Console, FDD/HDD full-PC
profile, and dependency-DAG gates passed.

The full-PC composition API header moved by `git mv` from
`vm/product/full_pc.h` to `vm/composition_full_pc.h`, matching its existing
root-composition implementation. Direct implementation and smoke callers now
use the composition path. The status contract remains at its current machine
header pending the separately planned root-status migration; no API or runtime
behavior changed. GCC, FDD/HDD full-PC profile, default-profile, VM-session,
retained Console, and dependency-DAG gates passed.

The VM CPU-probe header moved by `git mv` from `vm/product/cpu_probe.h` to
`vm/composition_cpu_probe.h`, matching the M5 composition-owned probe
implementation. The probe API, expected illegal-instruction diagnostic, and
CPU-stop boundary semantics are unchanged. GCC, CPU-probe, CPU-stop, retained
Console, and dependency-DAG gates passed.

The lifecycle composition header moved by `git mv` from
`vm/machine/machine.h` to `vm/composition_machine.h`, matching its root
composition implementation. Full-PC composition, retained Console, and
debugger now include the composition declaration directly; their machine
operations and UX are unchanged. The remaining product-to-machine edge is the
separate legacy `device.h` control surface. GCC, retained Console, CPU probe,
VM-session, FDD/HDD full-PC profile, default-profile, and dependency-DAG gates
passed.

The retained full Console is now a product-only parser and UX surface. Its
product-owned command target carries the existing initialization, lifecycle,
machine-info, display, debugger, recording, BIOS, RAM, and media operations;
VM root composition supplies the callbacks and retains the original default
media insertion and call ordering. The actual task artifact accepted piped
`HELP`, `INFO`, and `EXIT`, preserving the banner, prompt, help text, and
reported machine state. The dependency allowlist decreased from 4 to 2 entries;
GCC, all focused gates, and Console/debugger/FDD/HDD/session gates passed.

The shared core debugger target contract now declares typed register,
real/linear memory, port, execution, break/trace, and watchpoint operations
without a VM include. Its fake-target smoke verifies callback dispatch,
register write/read, break-count access, and context ownership. The next slice
binds this contract in VM composition before moving the retained debugger UI.

Linux Console now uses the same copied display, platform-input, and
platform-execution contracts as Win32. Its thread ordering, polling intervals,
and scan-code mapping are unchanged, and no Linux source imports VM machine
state. Source-DAG verification and the Windows GCC build passed; POSIX
GCC/ncurses compilation remains an explicit external verification gate.

Win32 Console and window thread files now use the VM-platform execution sink
for running state, flip observation, start, and stop. VM composition binds the
unchanged device operations after device initialization and clears them during
teardown. Thread creation, wait intervals, message-loop behavior, and window
close ordering are unchanged. The execution-sink smoke verifies start, stop,
state, flip, and unbinding. The dependency allowlist decreased from 6 to 4
entries; GCC, execution/input/provider, CPU-stop, retained Console/debugger,
default-profile FDD, full-PC FDD/HDD, VM-session, and product-session gates
passed.

The retained CMake graph now assigns `vvadp.c`, `vcmos.c`, and `vdebug.c` to
the VM lifecycle source list and leaves `nxvm-machine-core-executor` with only
`core/machine` sources. Shared `execution_context.c` now belongs to
`nxvm-runtime`, which VM lifecycle links, and the full-PC artifact receives the
same shared source explicitly. The two resulting single-owner targets were
removed from the CMake mixed-owner allowlist. GCC plus PC-device, CPU-probe,
CPU-stop, retained Console, FDD/HDD full-PC profile, default-profile,
VM-session, and source/target dependency-DAG gates passed.

The full-PC session lifecycle header moved by `git mv` from
`vm/product/full_pc_session.h` to `vm/composition_session.h`, matching its
existing root-composition implementation. Its direct implementation and smoke
caller now use the composition path; profile selection and lifecycle semantics
are unchanged. GCC, VM-session, FDD/HDD full-PC profile, default-profile,
retained Console, and dependency-DAG gates passed.

The VM session assembly header moved by `git mv` from `vm/product/session.h`
to `vm/composition_session_model.h`, matching the root-composition session
implementation. It continues to own registry, firmware, media, profile, and
debugger assembly while VM product remains UX-only. The session API and layout
are unchanged; its direct implementation and smoke include now use the root
path. This removes the product-to-profile edge and leaves 24 checked temporary
edges. GCC, VM-session, default-profile, retained Console, FDD/HDD full-PC
profile, and dependency-DAG gates passed.

The product utility split now leaves the root `type.*` foundation responsible
for the retained product-neutral C-runtime wrappers and `utilsLowerStr`.
Six core-machine sources include that root foundation directly, removing their
core-to-product include edges without changing their emitted diagnostics or
memory behavior. `vcpuins.c` intentionally remains on the legacy utility
surface because its HLT sleep requires the separately approved
`WAITING_FOR_INTERRUPT` composition boundary; it is not a mechanical move.
The migration map's VDM snapshot row now states the approved rule: the VDM
private snapshot embeds core text, and platform copies that core child object.

The VDM presentation boundary no longer exposes a core-machine snapshot from
`core/platform`. The VDM composition-owned snapshot preserves the timestamp
and embeds the copied core text child object; no platform payload names a
machine type. The VDM no-media presentation gate retains the same queued input
and text-capture result while the source dependency allowlist drops that
core-platform-to-machine edge.

The generic runtime registry no longer includes core-machine capability types.
Profile capability identifiers are plain unsigned values and the caller supplies
a query callback only when a profile actually requires one. The registry smoke
keeps the same CPU-manifest proof through a test-local callback; VM default
profile lookup remains capability-free because that descriptor declares no
requirements. This removes the core-product-to-machine source edge without
making registry selection a VM policy.

`STATUS` now lives only in root `type.h`. The legacy core-machine status
forwarder and all internal ABI/version fields in machine and registry
configuration have been removed; this repository is one synchronous build and
uses `version.*` only for product identity. The retained configuration values,
registry selection, and error results are otherwise unchanged.

The mixed-source `nxvm-baseline-runtime` CMake target is deleted. Its former
core-product utility, VM default-firmware profile, and VM platform sources now
compile in separate owner targets. A temporary source-free `INTERFACE` target
only preserves their retained legacy link relationship while the remaining
peer-call adapters are migrated; it cannot conceal a mixed source owner.

The default-profile `qdcga.h` no longer leaks the VM video-adapter
implementation to peer firmware sources. The adapter header moved to the
`qdcga.c` private include list; the profile header retains only its firmware
constants and declarations. This removes one profile-to-machine source edge
from the profile public surface; its implementation edge remains explicitly
allowlisted pending the VM composition video capability adapter. Display
behavior and call order are unchanged.

QDX special STOP and RESET now write core CPU pending requests rather than
including the VM device control surface. VM composition consumes reset before
stop immediately after the retained machine-refresh boundary, preserving the
legacy next-loop reset scheduling and safe-stop exit. The QDX opcode, printed
diagnostics, and device reset implementation remain unchanged.

The shared core block capability now carries HDD presence, geometry, and
sector transfer requests. VM composition binds it to retained `vhdd` after
device initialization and preserves its CHS current-pointer update inside the
adapter. Both QDDISK INT 13 transfer code and VBIOS reset-time parameter-table
construction use that same capability, removing their profile-to-machine
dependencies without changing media layout or BIOS data ordering.

The default-profile CGA firmware no longer calls the VM platform directly when
INT 10h changes a supported text mode. It now invokes a product-neutral core
display-mode notification; VM root composition binds that notification to the
unchanged `platformDisplaySetScreen` call after retained HDD initialization.
The mode fields are still written before the call, so the reconfiguration
remains synchronous at the original instruction path. GCC, dependency-DAG,
default-profile FDD, full-PC FDD/HDD, VM-session, and product-session gates
passed from freshly linked binaries; the allowlist dropped from 13 to 12
entries.

The retained video-adapter state moved by `git mv` from `vm/machine` to
`core/machine`. It contains only shared text-mode state and lifecycle stubs;
its two callers now include the core header. Its stale product and port
includes were removed because the header already supplies the required legacy
memory macros. This removes the last default-profile-to-VM-machine source
edge without changing the state layout or lifecycle call order.

The legacy trace declarations and implementation moved from
`core/product/utils.*` to the existing root `type.*` foundation. CPU and
assembler/disassembler callers retain the same types, macro expansion, and
function symbols, but `vcpuins.c` now includes only the root foundation. HLT
waiting moved from `vcpuinsRefresh` to VM composition between retained PIT
refresh and CPU refresh, preserving the old sleep-before-interrupt-check
ordering while keeping core free of host sleep calls.

VM composition now publishes a copied `vm_platform_display_frame` after reset,
after each retained machine refresh, and synchronously before the retained
platform screen reconfiguration on a supported INT 10h mode change. The frame
contains text, attributes, dimensions, cursor state, and the original
buffer/cursor change facts; it names no machine type. Both Win32 renderers now
read only that frame, removing their direct VM-machine includes. Linux uses the
same frame for painting but retains its separately allowlisted lifecycle and
keyboard ingress adapter. The dependency allowlist decreased from 10 to 8
entries. GCC, source-DAG, CPU-stop, retained Console/debugger, default-profile
FDD, full-PC FDD/HDD, VM-session, and product-session gates passed.

The retained `utilsSleep` symbol now delegates to a product-neutral
`core_product_wait` provider rather than including VM platform code. VM
composition binds the unchanged `platformSleep` implementation after platform
initialization and unbinds it before platform finalization; default-profile
firmware and debugger call sites remain unchanged. CMake now records VM
profile/platform dependencies on the core product utilities explicitly. The
new provider smoke verifies dispatch and unbound behavior. The dependency
allowlist decreased from 8 to 7 entries; GCC, provider, CPU-stop, retained
Console/debugger, default-profile FDD, full-PC FDD/HDD, VM-session, and
product-session gates passed.

Win32 keyboard mapping now uses the VM-platform input sink instead of including
VM machine state. VM composition binds its existing modifier reads, host-state
application, key delivery, and stop request functions after device
initialization and clears the binding during teardown. Mapping tables, the F9
fallthrough, and Console/window call order are unchanged. The input-sink smoke
verifies all four callbacks and unbinding. The dependency allowlist decreased
from 7 to 6 entries; GCC, input/provider, CPU-stop, retained Console/debugger,
default-profile FDD, full-PC FDD/HDD, VM-session, and product-session gates
passed.
