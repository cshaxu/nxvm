# M5 T48 S1: Naming Convergence Plan

Status: complete.

## Objective

Freeze the M5 naming-convergence task breakdown, artifact version rule, and
source-move order before any new file rename occurs.

## Scope

- Add the naming convergence plan for T48 through T54.
- Record the `0.5.NNNN` task-version and `build/output/nxvm_0_5_NNNN.exe`
  artifact convention.
- Clean current governance contradictions that would mislead the next M5
  implementation task.

## Non-goals

- No source file move, CMake target rename, runtime behavior change, or
  runnable artifact in S1. The T48 artifact is produced by S2 when the
  runnable rename task completes.
- No M6 DOS design work.

## Verification

1. Review updated governance documents for one active subtask.
2. Confirm the naming plan contains explicit stop conditions and artifact
   naming.
3. Confirm no source files changed.

## Stop Conditions

Stop if the task plan would require changing retained NXVM Console behavior,
debugger behavior, machine lifecycle ordering, or product behavior before the
owner approves that specific implementation task.
