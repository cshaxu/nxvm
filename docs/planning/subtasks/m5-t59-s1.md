# M5 T59 S1: VM Platform Ownership Audit

Status: complete.

## Objective

Audit `vm/platform/{win32,linux}` ownership while preserving symmetric
host-surface filenames such as `win32app`, `win32con`, and `linuxcon`.

## Scope

- Classify each VM platform file and notable function as shared host mechanism
  or VM policy.
- Keep `win32app`, `win32con`, and `linuxcon` names; a future `linuxapp` name
  is acceptable when a Linux app/window adapter exists.
- Identify only proven policy-free mechanisms eligible for T60 migration to
  `core/platform/{win32,linux}`.
- Produce `build/output/nxvm_0_5_0059.exe`.

## Verification

1. Platform ownership map is recorded.
2. No platform source move occurs in T59.
3. GCC builds `nxvm_0_5_0059.exe`.
4. Retained Console smoke and source-DAG gates pass.

## Result

Ownership audit is recorded in
`docs/verification/m5-t59-s1-platform-ownership-audit.md`. The audit keeps the
symmetric host-surface filenames and finds no safe whole-file T60 migration.
No platform source file was moved or renamed.

## Evidence

- Built `build/output/nxvm_0_5_0059.exe`.
- SHA256:
  `667C04CE1F8A42D9E1EF96A766C9FF54BF3DFEC9D7EDAD122FF9FC62C1B60C5D`.
- `nxvm-product-console-smoke`: passed.
- `verify-dependency-dag`: passed.
- `git diff --name-status -- src/vm/platform src/core/platform`: empty.
