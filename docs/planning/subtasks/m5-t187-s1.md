# M5 T187 S1: Bound Win32 Window Startup

Make Win32 app-run display/stop flags consistently interlocked and bound the
display-ready wait to five seconds. On readiness failure or timeout, retain the
existing stop, join, and finalize cleanup path.
