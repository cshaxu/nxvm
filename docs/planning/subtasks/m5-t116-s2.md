# M5 T116 S2: Vocabulary And Build Hygiene Governance

Status: complete.

## Result

The permanent source vocabulary and build-tree hygiene rules are recorded in
the source policy, execution policy, and contributor rules. Stale `build/asan`,
`build/ubsan`, and `build/mingw-gcc-x64` trees were removed after verifying
they were beneath `build/` and excluding `build/output/`.
