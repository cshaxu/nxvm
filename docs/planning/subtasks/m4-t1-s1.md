# Superseded Future Subtask

This pre-created future subtask was superseded by the dual-product roadmap
reset. It is not an active or eligible task.

Current rule: M3 must first close and produce the M4 breakdown. M4 now designs
firmware and the `nxvm.exe` product surface. The owned DOS design moved to M6
and will receive a fresh subtask record only when it becomes active.
