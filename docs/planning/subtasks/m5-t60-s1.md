# M5 T60 S1: Platform Migration Decision

Status: complete.

## Objective

Close the T60 platform migration decision after the T59 ownership audit while
preserving symmetric host-surface filenames.

## Scope

- Move only platform mechanisms proven to be shared and policy-free.
- If no such mechanism exists, record a no-move decision instead of forcing a
  risky migration.
- Keep VM execution/input/lifecycle, Console, window, and guest-control policy
  in `src/vm/platform`.
- Produce `build/output/nxvm_0_5_0060.exe`.

## Verification

1. Migration or no-migration decision is recorded.
2. Symmetric platform filenames remain stable.
3. GCC builds `nxvm_0_5_0060.exe`.
4. Retained Console smoke and source-DAG gates pass.

## Result

The no-move decision is recorded in
`docs/verification/m5-t60-s1-platform-migration-decision.md`. T60 does not move
platform source because the T59 audit found no safe whole-file policy-free
migration candidate.

## Evidence

- Built `build/output/nxvm_0_5_0060.exe`.
- SHA256:
  `14D0ACEA10EE3958EBFE41CACAB603FB214E5CF9B8DD26F714A2D197CB919FB5`.
- `nxvm-product-console-smoke`: passed.
- `verify-dependency-dag`: passed.
- `git diff --name-status -- src/vm/platform src/core/platform`: empty.
