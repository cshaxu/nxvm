# M5 T38 S1: BIOS/POST Authority Map

Map default-profile `vbios` firmware storage, POST registration, interrupt
table binding, reset vector, and direct callers before binding its sole object
through the live machine. Preserve default-profile ownership and the retained
POST sequence.
