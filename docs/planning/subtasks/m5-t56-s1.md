# M5 T56 S1: Correct Current/Future Documentation Facts

Status: active, not started.

## Objective

Correct documentation that presents future layout or aspirational foundation
types as current facts.

## Scope

- Document that `vm/main.c` exists now while `vdm/main.c` is a future VDM
  product entry point.
- Make the `type.h` description match current reality: `nxvm_core_status`,
  retained `t_*` aliases, C-runtime wrappers, trace helpers, and fixed-width
  compatibility typedefs.
- Preserve the naming rules and topology goals without claiming unimplemented
  VDM product entry behavior.
- Produce `build/output/nxvm_0_5_0056.exe`.

## Verification

1. Documentation fact scan has no current/future mismatch for `vdm/main.c` or
   `type.h`.
2. GCC builds `nxvm_0_5_0056.exe`.
3. Retained Console smoke and source-DAG gates pass.
