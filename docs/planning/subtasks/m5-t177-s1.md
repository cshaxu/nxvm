# M5 T177 S1: Direct Platform Run Ownership

## Scope

Replace the `vm_session` aliases for `vm_platform_run_context` and
`vm_platform_run_handle` with direct access to their embedded owners.

## Constraints

- Preserve the retained NXVM Console, debugger, worker lifecycle, and FDD boot
  behavior.
- Do not change platform backend ownership or introduce a second run path.
- Keep the task-level GCC artifact and remove the temporary build tree after
  verification.

## Acceptance

- No `platform_run_context_storage` or `platform_run_handle_storage` field or
  corresponding same-owner alias remains in `vm_session`.
- Lifecycle and display callers pass the address of the embedded member.
- Run-handle and two-window isolation smokes pass.

## Evidence

- Artifact: `build/output/nxvm_0_5_0177.exe`
- SHA-256: `7C2866B8DAFAB0C09B91589B5A3AEBF3DE278486E9371EBEFBFCE537A247849B`
- Gates: `M5:T139:S1:RUN-HANDLE:OK`, `M5:T149:S1:MULTI-WINDOW:OK`.
