# M5 T89 S1: VDM Machine Audit And Core Platform Contract

Status: active.

## Objective

Confirm that the current DOS-minimal machine has no mutable process state, then
define the product-neutral host-surface context and explicit exclusive-lease
contract for `core/platform`.

## Non-Goals

No VDM run loop, CLI, renderer migration, platform policy, or host integration.

## Gates

Record the VDM-minimal audit, add focused contract coverage, preserve the
dependency DAG, and stop for review if a proposed core contract needs a machine
snapshot type or product-specific policy.
