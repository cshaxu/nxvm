# M5 T94 S1: VDM Composition Audit

Status: active.

## Objective

Audit DOS-minimal composition for caller-owned context and session-lifetime
rules after the machine, platform, and product closure tasks. Change source
only for a demonstrated violation; do not add VDM UI or a speculative run
loop. The audit verifies `vdm/profile` immutability as a cross-cutting check.
