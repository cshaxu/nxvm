# M5 T183 S1: Embedded Control Ownership

Embed `vm_session_control_state` in `vm_session`; remove its heap allocation
and all same-owner pointer indirection. Its initialize/finalize APIs remain the
only lifecycle boundary for atomics and execution context.
