# M5 T11 S11: Hardware Debug-State Source Migration

## Objective

Move the original NXVM hardware debug-state source and header to
`machine/core` by `git mv`. Repair only direct includes and CMake ownership
required for the move.

## Non-Goals

Do not change the retained Console or debugger grammar, breakpoint/step/trace
semantics, output, or default workflow. Do not run the raw instruction recorder.

## Verification

Build the user-facing target and focused dependents; run the finite CPU probe,
FDD-backed execution-context lifecycle smoke, FDD/HDD reset-vector smoke, and
retained no-media Console/debugger interactions. Raw recorder runs remain
prohibited without a separately budgeted trace experiment.

## Stop Condition

Stop for owner approval before any user-visible behavior changes or any need
to enable recording.
