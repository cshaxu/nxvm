# M5 T186 S1: Remove Wait Sleep Alias

Replace all production and smoke uses of `core_product_utils_sleep()` with
`core_product_wait_milliseconds()`, then remove the zero-semantics alias.
Keep `core_product_wait_scope` and provider dispatch unchanged.

## Verification

- GCC task artifact
- core wait smoke
- debugger/Console lifecycle gate
- source scan showing no legacy sleep alias remains
