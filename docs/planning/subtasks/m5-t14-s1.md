# M5 T14 S1: Dependency Governance Reset

## Objective

Replace the prior local-debt completion rule with the product-composition DAG
defined in `docs/architecture/module-layout.md`, record the actual remaining
source and target crossings, and make it the M5 closure criterion.

## Non-Goals

Do not move runtime code, alter CMake targets, change the NXVM Console,
debugger, boot/media behavior, or begin M5 T14 S2.

## Verification

Review the source include inventory and CMake target map against the document.
Confirm the recorded current debt matches the source tree and that T13 S9-S11
are marked superseded rather than silently treated as complete.

## Stop Condition

Stop before any runtime or build-graph edit is needed.
