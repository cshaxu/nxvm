# M5 T13 S2: Full-PC Composition Name Cutover

## Objective

Rename the active `vm/product/baseline_full_pc.c/.h` pair and its internal API
to the final VM full-PC composition name by `git mv`, preserving exact code
logic and the retained NXVM Console/debugger behavior.

## Non-Goals

Do not alter lifecycle, media, execution boundary, keyboard behavior, Console,
debugger, CMake source composition, or any other historical-name owner.

## Verification

Build the user-facing artifact and focused full-PC consumers. Run the CPU,
FDD-backed execution-context, FDD/HDD reset-vector, no-media Console, and
delayed debugger gates. The source diff must be a name/path cutover only.

## Stop Condition

Stop for owner approval before an API change, behavior change, or a second
historical-name owner.
