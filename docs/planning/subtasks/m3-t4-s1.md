# M3 T4 S1: Isolate Platform Presentation

## Objective

Add adapter-owned timestamped input queues and copied text snapshots between a
platform-facing API and the DOS-minimal runtime shell. No platform code may
read guest state or include core internals.

## Non-Goals

No Win32 implementation, product window/Console behavior, CLI, or change to
the retained NXVM baseline is included.

## Verification

The focused mock test must print `M3:T4:S1:PRESENTATION:OK`; source scans must
show no platform include of core internals; M1 baseline must still build.
