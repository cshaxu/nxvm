# M5 T80 S5: Mode And Host Handles

Status: complete.

## Objective

Move platform display mode and Win32 Console/window handles into their owning
run contexts, retaining the NXVM display command and host behavior.

## Gates

Two-context overwrite smoke, Console/window FDD regression, retained Console
and FDD DOS-prompt gates. The facade scan rejects the retired global mode and
Win32 handle names in production sources.
