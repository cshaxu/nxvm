# M5 T109 S1: C And ISO C Facade Foundation

Status: complete.

## Objective

Define `C_*` scalar aliases and the `STD_*` ISO C facade. Rename existing root
wrappers and their active callers to `STD_*`; add missing wrapper surfaces
without changing behavior.

## Non-Goals

- Do not migrate scalar C keyword call sites yet.
- Do not introduce `WIN32_*` outside a later Win32-only task.
- Do not wrap `fflush(stdin)`.

## Verification

Build `nxvm_0_5_0109.exe`, all GCC gates, FDD DOS prompt, and retained
Console. Active source/tests must have no legacy root wrapper spelling.

## Result

`C_*` scalar aliases and `STD_*` ISO C wrapper/type/object surfaces exist;
all former root wrapper callers use `STD_*`. Evidence is
`docs/verification/m5-t109-s1-c-std-facade-foundation.md`.
