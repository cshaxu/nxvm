# M5 T58 S1: VM Session And Composition Symbol Naming

Status: complete.

## Objective

Rename low-risk VM session and composition public symbols whose prefixes still
mix legacy product names instead of current ownership.

## Scope

- Rename `nxvm_product_nxvm_session_*` and related config/type names to
  `vm_composition_session_model_*` where they belong to VM composition.
- Rename `nxvm_vm_full_pc_session_*` and related config/type names to
  `vm_composition_full_pc_session_*` where they belong to VM composition.
- Preserve Console text, media behavior, session creation/reset order, and
  smoke-test behavior.
- Produce `build/output/nxvm_0_5_0058.exe`.

## Verification

1. No mixed session prefixes remain in source or CMake. Passed.
2. VM session/product smokes pass. Passed.
3. Retained Console smoke and source-DAG gates pass. Passed.
4. `build/output/nxvm_0_5_0058.exe` builds, starts, and exits via `EXIT`.
   Passed.

## Artifact

- Path: `build/output/nxvm_0_5_0058.exe`
- SHA256:
  `4CA55250CBAB73BFFD5A6D8188FE737CA7B3DDF73BA3364B35B213F2EF9B0A15`
