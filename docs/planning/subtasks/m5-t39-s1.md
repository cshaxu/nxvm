# M5 T39 S1: QDX Authority Map

Map default-profile QDX dispatch-table storage, interrupt hand-off, stop
service, reset, and direct callers before binding its sole service object
through the live machine. Preserve dispatch semantics and firmware order.
