# M5 T13 S3: VM Request-Transport Name Cutover

## Objective

Rename `vm/platform/baseline_request_transport.c/.h` and its CMake/test names
to the VM request-transport owner by `git mv`, preserving the copied ingress,
egress, close/discard, and execution-boundary behavior exactly.

## Non-Goals

Do not change queue capacity, synchronization, producer coverage, keyboard
mapping, lifecycle, Console, debugger, or CMake composition outside this pair.

## Verification

Build the user-facing artifact and request-transport smoke. Run the CPU,
FDD-backed execution-context, FDD/HDD reset-vector, no-media Console, and
delayed debugger gates.

## Stop Condition

Stop for owner approval before a queue semantic or user-visible behavior change.
