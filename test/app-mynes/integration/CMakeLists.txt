add_executable(mynes-lifecycle-smoke lifecycle_smoke.c)
target_link_libraries(mynes-lifecycle-smoke PRIVATE mynes-core-driver common-machine storage base)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-lifecycle-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.integration.lifecycle-smoke COMMAND mynes-lifecycle-smoke)

add_executable(mynes-snapshot-transaction-smoke snapshot_transaction_smoke.c)
target_link_libraries(mynes-snapshot-transaction-smoke PRIVATE mynes-core-driver common-machine storage base)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-snapshot-transaction-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.integration.snapshot-transaction-smoke COMMAND mynes-snapshot-transaction-smoke)

add_executable(mynes-app-snapshot-commands-smoke app_snapshot_commands_smoke.c)
target_link_libraries(mynes-app-snapshot-commands-smoke PRIVATE mynes-app mynes-core-driver common-machine storage base)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-app-snapshot-commands-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.integration.app-snapshot-commands-smoke COMMAND mynes-app-snapshot-commands-smoke)

add_executable(mynes-debug-workflow-smoke debug_workflow_smoke.c)
target_link_libraries(mynes-debug-workflow-smoke PRIVATE mynes-core-driver common-machine storage base)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-debug-workflow-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.integration.debug-workflow-smoke COMMAND mynes-debug-workflow-smoke)

add_executable(mynes-app-debug-commands-smoke app_debug_commands_smoke.c)
target_link_libraries(mynes-app-debug-commands-smoke PRIVATE mynes-app mynes-core-driver storage base)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-app-debug-commands-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.integration.app-debug-commands-smoke COMMAND mynes-app-debug-commands-smoke)

add_executable(mynes-app-media-commands-smoke app_media_commands_smoke.c)
target_link_libraries(mynes-app-media-commands-smoke PRIVATE mynes-app mynes-core-driver storage base)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-app-media-commands-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.integration.app-media-commands-smoke COMMAND mynes-app-media-commands-smoke)

add_executable(mynes-controller-rom-smoke controller_rom_smoke.c)
target_link_libraries(mynes-controller-rom-smoke PRIVATE mynes-core-driver storage base)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-controller-rom-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.integration.controller-rom-smoke COMMAND mynes-controller-rom-smoke)

add_executable(mynes-visual-rom-smoke visual_rom_smoke.c)
target_link_libraries(mynes-visual-rom-smoke PRIVATE mynes-core-driver storage base)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-visual-rom-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.integration.visual-rom-smoke COMMAND mynes-visual-rom-smoke)

add_executable(mynes-motion-rom-smoke motion_rom_smoke.c)
target_link_libraries(mynes-motion-rom-smoke PRIVATE mynes-core-driver storage base)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-motion-rom-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.integration.motion-rom-smoke COMMAND mynes-motion-rom-smoke)

add_executable(mynes-ppu-timing-rom-smoke ppu_timing_rom_smoke.c)
target_link_libraries(mynes-ppu-timing-rom-smoke PRIVATE mynes-core-driver storage base)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-ppu-timing-rom-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.integration.ppu-timing-rom-smoke COMMAND mynes-ppu-timing-rom-smoke)

if(WIN32)
    add_executable(mynes-native-window-smoke native_window_smoke.c)
    target_link_libraries(mynes-native-window-smoke PRIVATE mynes-app mynes-core-driver
        common-machine common-session common-ui storage base)
    if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
        target_compile_options(mynes-native-window-smoke PRIVATE -UNDEBUG)
    endif()
    add_test(NAME mynes.integration.native-window-smoke COMMAND mynes-native-window-smoke)

    add_executable(mynes-native-console-smoke native_console_smoke.c)
    target_link_libraries(mynes-native-console-smoke PRIVATE mynes-app mynes-core-driver
        common-machine common-session common-ui storage base)
    if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
        target_compile_options(mynes-native-console-smoke PRIVATE -UNDEBUG)
    endif()
    add_test(NAME mynes.integration.native-console-smoke COMMAND mynes-native-console-smoke)
endif()
