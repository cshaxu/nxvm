add_executable(mynes-app-config-smoke config_smoke.c)
target_link_libraries(mynes-app-config-smoke PRIVATE mynes-app)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-app-config-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.app.config-smoke COMMAND mynes-app-config-smoke)

add_executable(mynes-app-command-smoke command_smoke.c)
target_link_libraries(mynes-app-command-smoke PRIVATE mynes-app)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-app-command-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.app.command-smoke COMMAND mynes-app-command-smoke)

add_executable(mynes-app-composition-smoke composition_smoke.c)
target_link_libraries(mynes-app-composition-smoke PRIVATE mynes-app-composition)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-app-composition-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.app.composition-smoke COMMAND mynes-app-composition-smoke)

add_executable(mynes-app-battery-failure-smoke battery_failure_smoke.c)
target_link_libraries(mynes-app-battery-failure-smoke PRIVATE mynes-app-composition)
if(CMAKE_C_COMPILER_ID MATCHES "GNU|Clang")
    target_compile_options(mynes-app-battery-failure-smoke PRIVATE -UNDEBUG)
endif()
add_test(NAME mynes.app.battery-failure-smoke COMMAND mynes-app-battery-failure-smoke)
